# 2026-08-29 工作日志

## 屏幕录制模块：崩溃修复 + 设置/面板统一 + UI 简化（4 项反馈）

### 1. 开始视频录制即崩溃 `0xc0000005 STATUS_ACCESS_VIOLATION`（根因级修复）

**现象**：点开始录制后进程直接退出，Rust 无 panic 信息；诊断日志停在
`[recorder] first frame captured: N bytes`，之后无任何输出。

**根因**：`src-tauri/src/dupl.rs` 的 DXGI 采集上下文缓存
`OUTPUTS: Mutex<Option<HashMap<(i32,i32), OutputCtx>>>` **只按显示器坐标作 key**，
而三个模块在各自 `std::thread::spawn` 的线程里调用同一份缓存：
- `screenshot.rs:174` capture_all（截图线程，每次截图新建线程）
- `scrollshot.rs:595` run（长截图线程）
- `recorder.rs:478` run（录制线程）

于是**录制线程复用了截图线程创建的 `ID3D11DeviceContext`** —— D3D11 immediate
context **不是线程安全的**，跨线程 Map/CopyResource 属未定义行为 → 访问违规。
日志佐证：先跑过截图（缓存已建）→ 再点录制 → 第一帧后崩。

**修复**：
- OUTPUTS 的 key 改为 `(ThreadId, i32, i32)`，每个采集线程独享 device/context/duplication。
- 新增 `dupl::win::release_thread()`（含非 Windows 空实现），按线程清理缓存。
- 三处采集线程入口各加 RAII guard，保证**所有退出路径**都释放：
  `recorder.rs` RecThreadGuard、`scrollshot.rs` ScrollThreadGuard、
  `screenshot.rs` capture_all 内 CapThreadGuard。
  ⚠️ 线程隔离后每个线程都会自建 D3D11 设备，**不释放就会每截一次图残留一套设备**（GPU 资源泄漏），三个 guard 缺一不可。
- `download()` 加固：Map 后校验 `pData` 非空且 `RowPitch >= stride`，不满足则
  **先 Unmap 再返回 None**（否则纹理一直挂 mapped 状态，后续采集全失败）。

### 2. 录制功能参数：帧率上限提高 / 去掉时长上限

- 帧率：`opts.fps.clamp(5, 24)` → `clamp(MIN_FPS, MAX_FPS)`（5~60）；
  **MP4 不再硬编码 30fps**，与 GIF 统一采用用户设置值。设置页滑块 24 → 60。
  达不到请求帧率时仍按真实间隔写时间戳（VFR），成品不会快进。
- 时长上限：删除 `RecCfg` / `load_cfg()` 与循环里的时长检查。
  ⚠️ 只改默认值不够——必须**停止读取** `max_duration_secs`，否则旧配置里的 120 仍会生效。
  字段保留仅为向后兼容，设置页已移除该项。
- 设置页删除「启用屏幕录制」开关（与功能开关页 `FeaturePage` 重复，统一由后者管理）。

### 3. 设置页与录制面板统一（同一套配置，两种用法）

- `RecorderConfig` 新增 `fmt`（"mp4"/"gif"）、`res`（"raw"/"1080"/"720"/"360"）；
  Rust `config.rs` + 前端 `types.ts` + `configStore.ts` 默认值同步，旧配置靠 `#[serde(default)]` 自动补齐。
- 设置页 = **持久默认值**（格式/分辨率/帧率/编码质量）；
  录制面板 = 只暴露最常用的「格式」，分辨率/画质/帧率沿用设置，面板改动只影响本次。
- `RecorderSelect` 从 `config_load` 读取全部默认值，含非法值兜底。

### 4. 录制浮层 UI 简化（功能不减）

- 选区配置面板：只留「格式」+ 重选/开始；删除「屏幕录制」标题与 `Enter/Esc` 快捷键提示；
  面板高度估算 214 → 120。
- 控制条改为**纯图标**：红点 + 时间 + 3 个 26px 圆形按钮（暂停/继续、停止、取消），
  语义靠 `title` 悬浮说明与配色表达；完成态的「打开」也图标化。
  **「格式错乱」的真实原因**：窗口固定 312×36 CSS px，而旧内容
  （红点+REC+时间+3按钮+「Esc 停止并保存」）约需 329px，**塞不下导致溢出**。
  去掉文字项后固定占用约 149px，余 163px 给时间，布局不再挤压。
- 删除废弃样式 `.recb-rec` / `.recb-rec.paused` / `.recb-esc-hint` / `.recb-folder` 的 padding。

### 验证
`cargo check` 通过（无警告）；`tsc --noEmit --incremental false` 通过；
`vite build` 通过（注：环境 safe-delete 会拦截清空 `dist/assets`，需加 `--emptyOutDir false`）。

### 待用户实测
- 录屏连续跑 2 分钟以上，确认不再崩溃（此前 120 秒上限也会自动停）。
- 帧率拉到 60 的实际落帧表现（受采集/编码能力限制，属预期内的 VFR 降级）。

---

## 第二轮反馈（4 条）

### 1. 录屏仍崩溃 0xc0000005 —— 上一轮根因不完整，真凶在编码器

上一轮修的「D3D11 跨线程」是真实隐患但**不是本次崩溃点**（新日志 `fps=24`
证明新代码已生效，却仍崩在第一帧之后）。第一帧采集后的路径只剩
`h264.rs::write_bgra`，复查发现：

**它每帧复用同一个 `IMFSample` / `IMFMediaBuffer`**，而 Media Foundation
SinkWriter 对已提交的 sample 是**异步消费**的——上一帧还在被编码器读取时就被
下一帧覆写，即典型的访问违规。

修复：
- `write_bgra` 改为**每帧新建 buffer + sample**（`new()` 里那对只作初始化探测）。
- Lock 后校验 `pData` 非空且 `cbMaxLength >= frame_bytes`，不满足则先 Unlock 再返回
  （否则 buffer 一直挂锁定状态，后续写帧全失败）。
- `recorder.rs` 记录写帧失败原因 + 前 3 帧各打一条 `[recorder] frame N encoded`，
  下次再崩就能从日志区分死在「采集后」还是「编码后」。

### 2. 录制面板再简化为一行

去掉「重选」（右键即可清空选区重选，功能不丢）、去掉「格式」标题；
格式由下拉框改为**平铺按钮**（Video/Image 图标 + MP4/GIF 短标签，选中态品牌色实底），
与「开始录制」同排，中间用竖线分隔。面板宽度改由内容撑开（`width: auto`），
宽高估算 320×214 → 250×62。
CSS 注意：`.rec-start` 原来靠 `.rec-actions .rec-start` 后代选择器生效，
改结构后必须同步改选择器，否则丢失样式。

### 3. 悬浮工具栏去阴影

工具栏本体早就是 `shadow:false` + 纯内高光。**阴影实际来自贴边收起时的
`toolbar-edge-breathe` box-shadow 外发光**（0 0 10px/22px）。
改为只让亮轨 `border-color` 明暗呼吸，保留可发现性但不再发光。

### 4. 悬浮提示被工具栏挡住 —— 独立窗口方案

根因：提示用的是**原生 `title`**，它是 WebView 的子窗口，z-order 永远低于
`alwaysOnTop` 的工具栏，显示出来就被自己压住。

**走过的弯路（重要教训）**：一度想扩大工具栏窗口、在窗口内预留提示区，
但**贴边收起是"把窗口滑出屏幕只露一条边"**——窗口一旦大于工具条，
贴底收起露出的就是透明的预留区而不是工具条，直接破坏收起功能。
（贴底收起只能露出窗口顶部、贴顶只能露出窗口底部，二者对条的位置要求相反，
无解。）已全部回退。

最终方案：**独立置顶提示窗口**（新增）
- `tauri.conf.json` 新增窗口 `toolbar-tip`（transparent / 无装饰 / 无阴影 /
  alwaysOnTop / skipTaskbar / 默认隐藏）。⚠️ 字段名是 `focus` 不是 `focused`，
  写错会导致 tauri-build 直接失败。
- 新增 `modules/toolbar/tip.ts`（事件通道 `toolbar://tip`）、
  `ToolbarTip.tsx`（监听 → 测量 → 置顶定位 → 显示）、`tip.css`。
- `Toolbar.tsx`：hover 按钮时把**按钮中心的屏幕坐标**（`outerPosition()/scaleFactor()`
  换算成 CSS 像素）emit 出去；鼠标离开 emit null 隐藏。
- 定位：横条→提示在按钮上方居中，竖条→在按钮右侧垂直居中；再按
  `screen.avail*` 夹回工作区。每次显示都 `setAlwaysOnTop(true)`，
  保证两个 TOPMOST 窗口之间提示浮在上面。

### 验证
`cargo check` 通过；`tsc --noEmit` 通过；`vite build` 通过。
⚠️ 环境注意：`vite build` 写入已存在的 `dist/` 文件会 EPERM（应用占用 +
safe-delete 保护），需输出到**全新目录**（如 `dist_check2`）才能验证；
临时目录因环境回收站不可用无法自动删除，需手动清理。

---

## 第三轮反馈（5 + 1 条）· 本轮【未经编译验证】由用户自行编译

> ⚠️ **教训（AI 造成的事故）**：绝不要在 `src-tauri/` 下创建临时
> `CARGO_TARGET_DIR`（本轮用了 `src-tauri/target_verify`）。它会落入
> tauri dev 的文件监视范围——目录内产物一变就触发 rebuild，rebuild 又改动
> 产物，导致 dev **无限重编译循环**（用户表现为启动后一直 Rebuilding）。
> 需要旁路编译时，target 目录必须指到项目外；dev 进程持有
> `.cargo-build-lock` 时也应直接交由用户编译，不要自建目录硬刚。
> 事后已终止相关进程并清理该目录（178M）。同类临时目录（dist_check2/3）
> 留在项目根不会触发 dev 重建，但仍属垃圾需手动清理。

### 0. MP4 仍崩溃 —— 真凶锁定为「硬件编码器驱动」

上一轮加的打点立功了，日志出现 `frame 1 encoded` 却无 `frame 2`：
**第 1 帧编码成功、第 2 帧死在 native 内部**（Rust 无法捕获）。
同时日志显示 GIF 完整录完 97 帧 —— 采集链路（上上轮修的线程隔离）已正常，
问题只在 MP4 编码路径。

结论：`MF_READWRITE_ENABLE_HARDWARE_TRANSFORMS=1` 启用的 GPU 硬件编码 MFT，
在连续快速提交 BGRA 帧时会驱动级崩溃。
**修复：删除该标志，强制走软件编码器**（Microsoft H.264 Encoder MFT），
屏幕录制这种分辨率/帧率对 CPU 毫无压力，稳定性远高于各厂商驱动。
（改为软件后创建属性数量 2 → 1，同步改了 MFCreateAttributes 的容量参数）

### 1. 启动速度：选区窗不再销毁，改为复用

原流程每次录制都 `window.close()` 销毁选区窗，下次呼出要重建 WebView（一两秒）。
现改为**只 hide 不销毁**：
- `recorder_start` 不再 close 选区窗
- `finish()`（所有结束路径）统一 hide + 恢复鼠标事件
- `rec_select_cancel` / `dismiss_select_if_open` / `rec_dismiss` / `on_bar_destroyed` 全部 close → hide
- `begin_select` 复用分支：`show` + `set_focus` + emit `recorder://select-reset` 通知前端清空状态

### 2. 录制中加黑色遮罩 + 拖拽框改虚线

- `recorder_start` 成功后：选区窗 `set_ignore_cursor_events(true)`（鼠标穿透，
  不挡用户操作桌面）+ emit `recorder://mask`，前端进入遮罩模式渲染
  `rgba(0,0,0,0.38)` 四块遮罩镂空录制区 —— 与选区阶段视觉统一
- `.rec-frame` 由 1.5px 实线改为 **dashed**，对齐原生边框环（recframe.rs 的
  6px 实/4px 空密集虚线）；`.rec-corner` 去掉 `drop-shadow` 发光
- 遮罩模式下不渲染配置面板；Enter 在遮罩模式被忽略（防重复 start）

### 3. 控制条（rec-bar）整体移除 + 新的停止入口

- 删除 `ensure_bar` / `bar_phys_size` / `BAR_W` / `BAR_H` / `apply_select_blur`
  （BAR_LABEL 保留给 rec_dismiss 兜底）
- **再按一次录制快捷键/托盘/工具栏 = 停止**：`begin_select` 检测到 ACTIVE 时
  直接 `STOP.store(true)`（原来只是 return）
- 遮罩窗仍持有焦点时按 Esc 也可停止（前端 `recorderStop()`）
- `.rec-hint` 去掉 `<kbd>Enter</kbd>/<kbd>Esc</kbd>`，只留「拖拽框选录制区域」

### 4. 面板按钮瘦身

- MP4 / GIF 去掉左侧图标，只留纯文字（import 的 Video/Image 一并移除）
- 「开始录制」去掉文字，改为**红色圆形录制点**纯图标按钮（30×30）
- 格式按钮 padding 6px 11px → 4px 10px，底色更轻

### 5. 悬浮工具栏去阴影（再次）

上一轮改的是收起态呼吸的外发光（改为 border-color 明暗）。
本轮在 **DWM 层彻底禁用**：`show_toolbar_initial` 中对工具栏 hwnd 设置
`DWMWA_NCRENDERING_POLICY = DWMNCRP_DISABLED(1)`。
线索来自 `toolbar_apply_clip` 的注释「恢复完整窗口与 DWM 阴影」——
说明 conf 的 `shadow:false` 在无边框窗口 + SetWindowRgn 往返下并不干净。

### 待用户验证
- MP4 录制（软件编码）是否还有崩溃 —— 若仍崩，日志会明确停在 `frame N encoded` 之后
- 二次呼出录制是否即时（复用生效）
- 工具栏是否彻底无-shadow

---

## 第四轮反馈（4 条）· 本轮同样未经编译验证

> ⚠️ **上一轮理解偏差（重要教训）**：用户说「去掉开启录制视频时顶部的提示栏」时，
> 我删掉了 rec-bar 进度条，但用户实际是要**保留**它（他要去掉的是别的）。
> 这轮明确要求「进度条回来」。
> 教训：涉及"去掉某个可见元素"的需求，若该元素承载唯一交互入口
> （此处是停止/暂停/取消），必须先确认再动手，不要直接删。

1. **恢复顶部进度条（rec-bar）**：从 925ffdf 用 `git show` 取回被删的
   `ensure_bar` / `bar_phys_size` / `BAR_W` / `BAR_H` / `apply_select_blur`，
   并在 `recorder_start` 重新调用。窗口保留 `exclude_from_capture`
   （控制条不会被录进视频）+ SWCA 亚克力。
   「再按一次快捷键 = 停止」的兜底入口**保留**（双保险）。

2. **遮罩模式不再画前端框**：录制中前端只渲染黑色遮罩，描边交给原生
   边框环（recframe.rs）。此前前端虚线框与原生环两套重叠；
   顺带解决「左上角尺寸标签一直不消失」（`.rec-size` 随 frame 一起不渲染）。

3. **三处浮层统一到截图工具栏令牌（--tb-\*）**
   令牌 `--tb-bg / --tb-fg / --tb-hover-bg / --tb-line / --tb-border / --tb-shadow`
   定义在 **screenshot.css 的 html 层**（只在 html 上设，绝不写在子容器，
   否则会遮蔽主题值）；该 CSS 被 import 进同一 bundle，所有窗口都能用。
   - `.recb-root`（进度条）：`color-mix(in srgb, var(--tb-bg) 76%, transparent)`
     + `border-radius: 12px` + `box-shadow: var(--tb-shadow)`
   - `.recb-toast`（完成通知卡）：圆角 `--radius-panel` → `12px`
   - `.rec-panel`（选区右下角配置面板）：padding 改 `8px 10px`
     （对齐 `.shot-toolbar-panel`）、圆角 12px、`--tb-shadow`
   - 按钮/竖线/文字全部换成 `--tb-*`；`PANEL_H_EST` 62 → 52

   ⚠️ **模糊必须走系统层**：rec-bar 是透明 Tauri 窗，CSS `backdrop-filter`
   采不到桌面（项目禁用，4K 下会冻结窗口）——"高度模糊"由 Rust 的
   `apply_blur`（SWCA AccentPolicy BLURBEHIND）提供，前端只做半透明底。
   `.rec-panel` 的 backdrop-filter 采的是**页面内黑色遮罩**（页面内元素），
   因此可用并保留。

---

## 第五轮：录屏音频（阶段 B）完成 + 阶段 A 画质调参入库

用户反馈「加上的音频录制功能似乎没有做完」。

### 现场诊断：孤儿模块

`src-tauri/src/recaudio.rs` **379 行早已写完**，但 `lib.rs` 里**从来没有
`mod recaudio;` 声明** —— 它从未参与编译，`cargo check` 一直 0 错误正是
因为它压根没被构建。这解释了「似乎没做完」：代码写完了，接线一行没做。

> **教训**：判断「某功能做到哪一步」时，先看 `lib.rs` 的 `mod` 声明列表，
> 别只看文件是否存在。未声明的模块 = 未编译 = 里面可能全是 API 签名错误。

阶段 A（画质）其实也已在位但未提交：`h264.rs` 的 `EncTuning`、
`MF_MT_MPEG2_PROFILE`、ICodecAPI 双 guidService 探测、四个码控参数，
以及 `examples/recq_probe.rs` 探针。

### 音频接线（本次实际新增）

- **h264.rs**：`new_ex` 增加 AAC 音轨（AAC 输出 + PCM s16 输入，
  必须在 `BeginWriting()` **之前** AddStream/SetInputMediaType）；
  新增 `write_pcm16` / `audio_ts` / `video_ts`。
  ⚠️ 音频失败一律走 `unwind(Err(...))`（先 MFShutdown + CoUninitialize），
  否则 recorder 用 None 重建时会撞上未配对的 MFStartup。
- **时间戳用累计精确式** `frames * 10_000_000 / rate`，
  **绝不能用** `ts += 每包时长`（1024 帧 = 213333.33 个 100ns，
  零头持续累积，几分钟后音画脱同步）。
- **recorder.rs**：`RecOpts.audio` + 音源入参；录制前起 AudioEngine，
  每帧 `write_bgra` 成功后 `drain`，暂停 `discard`，收尾**先 finish 再 finalize**；
  带音轨 writer 创建失败 → 用 None 重建（音频绝不阻断录制）。
- **架构铁律**：音频采集线程**只产 PCM 包**（mpsc），**绝不碰 IMFSinkWriter**
  —— 它在录制线程创建，MF 对象要求串行化。

### windows 0.61 API 踩坑（recaudio.rs 首次编译时暴露）

- `IMMDevice::Activate()` 需 2 参：`Activate::<IAudioClient>(CLSCTX_INPROC_SERVER, None)`
- `AUDCLNT_STREAMFLAGS_LOOPBACK` 是 **u32 常量**，不是 newtype（没有 `.0`）
- `IAudioCaptureClient::GetNextPacketSize()` **无参**，直接返回 `Result<u32>`
- `CoInitializeEx` 需 `unsafe` 包裹
- `mpsc::Receiver` 不可克隆：原先 `Cap` 同时持有 `rx` 又要 `caps.push(cap)`
  → 部分移动编译错误。改为 `Cap` 只留 `stop + handle`，`rx` 单独由
  engine 的 `mic_q / sys_q` 持有。
- `MMDeviceEnumerator` 无 CoClass，`CLSID` 写字面量
  `{BCD5D384-ACF6-44F3-BB41-0989C731EDA7}`

### 验证（不靠人耳）

新增 `examples/audio_probe.rs`：合成画面 + 440Hz 正弦 PCM 录 3 秒，
再**解析 MP4 box 树**断言。实测通过：

```
track handler : ["vide", "soun"]
视频轨 3.000s / 音频轨 3.008s（差 0.27%，阈值 15%）
PCM 峰值 8000（> 500，证明喂的不是静音）
```

`hdlr` 的 handler_type 在 box 内偏移 **+16**（version+flags 4B + pre_defined 4B）；
`mdhd` v0 的 timescale 在载荷 +12、duration 在 +16。

新增 4 项单测（混音削波到 ±32767、累计时间戳防漂移、44.1k→48k 上采样、
48k 恒等）。`cargo test --lib` **24 项全绿**，`cargo build` **0 警告**。

### 顺带处理的仓库卫生

- 上一轮遗留未提交改动里，**「任务栏透明」（taskbar.rs + TaskbarPage.tsx +
  acrylic.rs 的 apply_accent 抽取）与本次音频改动在 config.rs / types.ts 中
  相互交织，无法干净拆分**，已一并提交并在 commit message 中说明。
- `git add -A` 误把 `dist_check2/ dist_check3/ dist_verify/`（26 个临时构建产物）
  入库 —— `.gitignore` 原本只有精确匹配的 `dist`。已补 `dist_check*/` 与
  `dist_verify/` 规则并 `git rm --cached` 移除（文件保留在磁盘）。

### UI

设置页音源 Segmented（不录音/麦克风/系统声音/两者）；
选区面板音源按钮（仅 MP4 显示，关/麦/系/混）；
控制条静音按钮（仅本次带音轨时出现，`AUDIO_STATE` 决定显隐）；
`BAR_W` 312 → 356 为新按钮留位。静音 = 写零帧不断流，音画时间线不脱开。

---

## 第六轮：便签重新集成（从 28dbce5 移除后恢复 + 按新要求定设置归属）

用户要求把独立项目 XiaoxinStickyNote 重新集成进工具箱，且：
- 便签设置**单独设置面板**、几乎全部独立于工具箱统一配置
- 翻译 API 等**不用配两遍**（用工具箱通用配置）
- 主题便签自己配；设置面板 UI 与工具箱统一；功能全移植

### 关键背景：便签不是没做过，是被整体移除过

git 历史：`dfb706b` 集成 → `28dbce5`（2026-08-20）**整体移除**（40 文件、
16504 行）。移除时工具箱版（2025 行 note.ts）比独立项目（1967 行）**还新**，
且已含独立项目之后的所有适配 + 工具箱专用 `anim-loader.ts`（动画懒加载）。
→ 恢复策略：**从 28dbce5^ 恢复文件**，而不是从独立项目重新移植。

### 恢复与重新接线（本次实际改动）

- `git checkout 28dbce5^ -- src/sticky src-tauri/src/sticky.rs
  src/settings/StickyNotePage.tsx`（24 前端文件 + 1883 行 sticky.rs + 392 行设置页）
- **lib.rs**：`mod sticky` + `use tauri::Listener` + 启动迁移
  `migrate_legacy_sticky` + 快捷键分发短路 `handle_sticky_shortcut`（在工具箱
  快捷键之前判断，命中即 return）+ setup 里 `register_all_shortcuts`（**只增量注册、
  不 unregister_all，避免注销工具箱热键**——旧版已处理过这个坑）+
  `ensure_particles_window` + 41 个 `sticky::*` 命令注册
- **acrylic.rs**：补回 `ACCENT_ENABLE_ACRYLICBLURBEHIND(=4)` 常量与
  `apply_acrylic_tinted`（tint 走 ABGR：`(1<<24)|(b<<16)|(g<<8)|r`，flags=0 亚克力专用）；
  sticky.rs 的 `clear_acrylic` → 重构后的 `clear_blur`
- **panel.rs**：`panel_active` 把 `sticky-history` 与可见 `note_*` 计入"面板打开"
  （工具栏图标点亮）；`panel_toggle` 加 `"sticky"` 分支（toggle 历史窗口）
- **tauri.conf.json**：补回 `particles` 全屏透明粒子层窗口声明（1920×1080，
  与用户屏幕一致；运行时 `ensure_particles_window` 兜底）
- **capabilities/default.json**：windows 列表补 `note_*` / `particles` /
  `sticky-history` / `sticky-imageviewer` / `sticky-settings`——Tauri v2 中
  未匹配任何 capability 的窗口拿不到 core:window/event 权限，便签会收不到事件
- **前端**：main.tsx 恢复按 label 分流（`note_*`/`particles`/`sticky-*` 提前
  `return`，**不加载 base.css**，避免便签被工具箱不透明底色污染）+ `view=sticky-settings`
  iframe 模式；SettingsApp 加便签设置页（固定项，不受功能开关控制）；icons 恢复
  IconSticky；Toolbar 恢复 sticky 入口 + `note_*` 前缀映射；types.ts ToolKey 补
  "sticky"；configStore 默认工具栏补 sticky；theme.css 补 `--tool-sticky`（注意
  theme.css 已被重构为 **light-dark()** 写法，只需 base 行 + 各彩色主题块各一行）

### 设置归属决策（按用户要求判定）

| 设置 | 归属 | 理由 |
|---|---|---|
| 外观主题/背景图/沉浸/毛玻璃/透明不透明度 | 便签独立 | 便签专属外观，工具箱无对应项 |
| 关闭动画（粒子模式/强度/速度） | 便签独立 | 便签专属 |
| 贴边收起、存储目录、Markdown 主题/CSS | 便签独立 | 便签专属 |
| LLM（整理格式，OpenAI 兼容） | 便签独立 | 工具箱没有大模型配置 |
| 快捷键 | **UI 统一、存储独立** | 便签快捷键支持纯 F 键/方向键（`stickyComboFromEvent`），
  存便签自己的 `sticky_settings.json` 的 shortcuts 字段，保存后调 `register_shortcuts`
  重注册；原 ShortcutPage 已被拆成各模块页，便签快捷键录入组件移植进 StickyNotePage |
| 翻译 | **无重复配置** | 便签只有每张便签的"翻译区显示开关"（NoteData.translate），
  设置里根本没有翻译 API 配置；工具箱的 youdao/baidu 是翻译面板用的，二者不冲突 |

### 教训

1. 判断"功能做到哪一步"先查 git 历史（`git log --all --diff-filter=D`），
   可能不是"没做过"而是"被移除过"——被移除的成熟模块从 git 恢复比重新移植快得多
2. Tauri v2 capabilities 的 windows 列表**按窗口标签白名单**，新窗口类型必须加进去，
   否则核心 API（window/event）静默不可用
3. 恢复旧代码不能机械回退：theme.css 已从「每主题整块覆盖」重构为
   `light-dark()` 单规则双态，要用新规范补令牌而不是照抄旧块

---

## 第五轮反馈（2 条）· 均为前端修复，tsc 通过

### 1. 快速文件：索引更新时间不变化 / 显示 C:/ D:/ / 条数主题色
- 更新日期恒不变化的根因：Rust 侧 `built_at` 是**毫秒**（now_ms），前端调用
  `agoLabel(status.built_at / 1000)` 转成了**秒**，而 agoLabel 内部 `Date.now() - ts`
  期望毫秒 → diff 恒为天文数字，永远走 dateLabel 分支且 `new Date(秒)` = **1970 年 1 月**，
  所以日期看起来从不更新。修复：直接传毫秒。
- 去掉 `status.roots.join(" ")`（C:/ D:/ 根目录显示）。
- 索引条数用 `--accent` 主题色（新增 .qf-index-count）。

### 2. 便签集成：设置侧栏点「便签设置」显示功能开关页
根因：SettingsApp 的 `currentPageDisabled` 判定只排除 features/general/about，
漏了同为**固定页**的 sticky——moduleItems（受功能开关控制的模块列表）里没有
sticky（它在 FIXED_ITEMS），于是 page==='sticky' 被判为"已停用的模块页"，
整个内容区被 `<FeaturePage />` 替换。
修复：固定页集合（FIXED_ITEMS 的 key）一律不参与停用判定，后续加固定页不会再踩。
（顺带排查确认：便签设置入口链路本身是通的——main.tsx 分流 sticky-settings
独立窗口、open_settings_window、侧栏 sticky 项均正常。）

---

## 第六轮反馈（3 条）· 便签模块，tsc 通过

### 1. 壁纸：便签面板不显示（只有历史面板有）
根因：历史面板调 `applyPanelBackground(root, s)`（内部透明主题也会取系统壁纸），
而便签本体在 **transparent 主题**下直接走亚克力分支、**不应用壁纸图** → 同一张
壁纸只有历史面板看得见。修复路径：用户要求的"去掉透明主题"（见 3）顺带解决——
存量 transparent 迁移为浅色后，便签走非透明分支即显示壁纸。
「壁纸预览展示不全」：设置窗内 `.bg-img-preview` 是 `background-size: cover`
（裁切）→ 改 `contain` 完整展示。

### 2. 新建便签默认隐藏格式工具栏
note.ts：HTML 初始 `aria-pressed` true→false、`.toolbar` 加 `display:none` 兜底
（消除首帧闪现）；加载/新建分支默认值 `?? true`→`?? false`。
老便签若存过 toolbar_visible 仍按其值显示。

### 3. 便签主题：去掉「透明」，只留浅色/深色
- settings.ts：THEME_GROUPS 删除"透明"组；load 迁移 transparent → light 并回写
- StickyNotePage.tsx：Segmented 去透明选项 + desc 更新 + load 迁移回写
- 理由：工具箱本身就有不透明度 + 亚克力，便签透明主题冗余。
