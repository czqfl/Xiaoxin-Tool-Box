# 2026-08-13 工作日志

## 端口工具面板：关闭端口后成功反馈
- 需求：用户在 PortPanel 点「终止」杀掉占用端口的进程后，没有任何明确成功提示（只有重查间接显示）。
- 改动：`src/modules/port/PortPanel.tsx` 新增 `feedback` 状态；`handleKill` 在 `await killPort` 成功且重查后写入「已成功终止进程「name」（PID x）」；`runQuery` 开头清 feedback；body 渲染绿色 `.port-feedback` 横幅（优先级高于错误提示，避免与「未被占用」误导文案叠加）。CSS 加 `.port-feedback`（复用 `--success`/`--c-green-soft`，与 credential/folder 成功态一致）。
- 验证：`tsc --noEmit` exit 0；自验执行路径无竞态（setFeedback 在 await runQuery 之后，不会被其清空）。
- 提交：`deef18e`（未 push）。

## 全仓库隐藏 bug 排查与修复
- 触发：用户要求「排查项目中隐藏的 bug」。先由 Explore 子代理初筛 8 项，再逐条对照源码核准。
- 确认 7 个真实 bug（1 个误报 #1 trim_entries：Vec 始终 insert(0) 最新在前，created_at 仅用于顺序队列排序，删除最旧无误）：
  1. #3 translate.rs `read_selection_clipboard` 注入 Ctrl+C 前未置 SUPPRESS_WATCH → 划词兜底复制把选中文本写进剪贴板历史（隐私/脏数据）。修复：send_ctrl_c 前置位，还原处已置位。
  2. #4 clipboardStore `toggleFavorite/togglePin/updateText` 乐观更新无回滚。修复：捕获失败回滚 prev。
  3. #6 folder.rs `folder_open*/folder_open_in_editor` 更新访问计数后未 emit(EVT_CHANGED) → 面板不刷新。修复：persist 后 emit。
  4. #2 多个前端面板 `onEvent().then(un=>cleanup.push(un))` 异步卸载竞态（监听器泄漏）。App/Toolbar/ShortcutPage 已有 disposed 守卫；为 TranslatePopup/usePanel/FolderPanel/CredentialPanel/ClipboardPanel/SettingsApp 补上。
  5. #8 关闭翻译弹窗（×/失焦）未复位 TRANSLATE_POPUP_OPEN → 系统级 Esc 一直被兜底拦截。新增 `translate_popup_close` 命令（复位+隐藏），前端 closePopup/失焦统一调用。
  6. #7 explorer.rs 基线 HashSet 大小写敏感 vs register_visit 大小写不敏感 → 同目录不同大小写被重复计数。修复：基线/比较改小写键。
  7. #5 `core/tauri.ts` safe() 静默吞错使面板空白。修复：失败时写入 diag_log 便于排查（仍 fail-soft）。
- 验证：`cargo check` 通过；`tsc --noEmit` exit 0。
- 提交：7 个独立 commit（#3 68c55f8 / #8 7bb9d6c / #7 1f259ca / #4 101f7ac / #6 6f3c5cd / #5 99621b8 / #2 392e0e9），均未 push。
- 注意：并行跑多个 `git commit` 会因 `.git/index.lock` 冲突导致提交合并/失败；提交必须串行。
