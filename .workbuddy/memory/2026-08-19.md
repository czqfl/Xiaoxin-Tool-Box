# 2026-08-19

## 历史窗口背景不生效 + 状态更新慢（commit b39ff96）
- ①根因：history.ts 监听 "xiaoxin-sticky-note-settings-changed"——那是原版
  settings.ts 窗口内 CustomEvent（window.dispatchEvent），不是 Tauri 跨窗口
  事件；工具箱集成版 save_settings 广播 "settings-changed"。事件名不匹配 →
  历史窗口永远收不到 → 背景/主题不实时刷新。改监听 "settings-changed"。
- ②状态更新慢：轮询 2s→1s + 不再随失焦停止（失焦 stopPoll 是"打开中"徽标
  延迟出现的根因；render 有签名去重开销极小）。
- ③便签窗口 onSettingsChanged 回调加 diag_log（验证事件到达，
  若"改了设置不实时生效"是事件未到达，日志 [note] settings-changed fired 立现）。
- 便签窗口实时链路（onSettingsChanged→notifyChanged 重读磁盘→applyTheme/
  applyBackground/applyGlassEnabled）代码上已完整，待日志验证。
- 验证：tsc + vite build 通过；已 commit 未 push。

## 便签"每次打开变大一圈"——尺寸单位混用（commit 8e42515）
- 实测存档 6768cc：w=1296 h=1626（默认 420×440，滚雪球放大 3 倍）。
- 根因链：Rust close_window 关闭便签时回写 outer_size()（物理像素）到存档 →
  下次 ensure_note_window inner_size(物理) 当逻辑用 → 物理×scale(1.5) →
  前端 onResized 的 window.innerWidth 在 150% 缩放环境也返回物理值 → 再存
  更大的物理值 → 每次打开 ×1.5。
- 修复：①close_window 回写 size 转逻辑（÷scale；pos 保持物理，ensure
  position(pos/scale) 一致）；②note.ts 尺寸保存改 outerSize/scaleFactor 算
  逻辑像素；③损坏存档 ÷1.5 修正 864×1084。
- 历史状态刷新：轮询 1s → 500ms。
- 教训：Tauri 2 尺寸单位——inner_size/LogicalSize/position 逻辑、outer_*
  物理；window.innerWidth 在高缩放环境可能返回物理值，勿用。
- 验证：tsc + vite + cargo build 通过（exe 13:36）；已 commit 未 push。

## 历史状态更新慢——关闭动画时序（commit 493d302）
- 根因：点 × → 粒子动画（0.5~1s+）→ finishClose 才 markNoteClosed → 广播 →
  历史刷新。用户感知的"慢"= 动画时长。
- 修复：requestAnimatedClose 开头（点 × 瞬间）立即 markNoteClosed 广播，
  finishClose 移除重复标记；呼出打断时 mark_note_open_inner 自动恢复。
- 诊断：history.ts state-changed 监听加 diag_log（[history] state-changed
  received）；若缺失 = 事件未投递（查后端 emit）。
- 验证：tsc + vite build 通过；已 commit 未 push。

## 历史便签新建按钮美化（commit c81623e）
- 标题栏 + 按钮：小方块 → 圆形品牌色，hover 旋转 90° + 光晕。
- 空状态：居中大号新建按钮（56px 虚线圆环 ➕ + 文字），hover 实线化抬升；
  createNewNote 抽公共函数，标题栏与空态共用。
- 纯前端（history.ts + styles.css）；tsc + vite build 通过；已 commit 未 push。

## 新建按钮居中（commit 2639eb9，修正 c81623e 理解偏差）
- 用户要求"新增便签按钮居中显示"——指标题栏右上角的 + 按钮，
  我上轮只把空状态大按钮居中了（用户面板有列表时看不到）。
- 修正：标题栏移除 + 按钮；新建按钮移到历史面板顶部居中（胶囊按钮
  ➕ 新建便签），有列表/空列表都居中；空状态大按钮保留。

## 标题栏按钮圆形化（commit 11d3d6d）
- 用户要求：新建按钮回标题栏（与关闭同排）、关闭/置顶/全屏/缩小全圆形、
  悬浮样式与新建按钮一致。
- .history-window/.note-window 标题栏 icon-btn 统一 26px 圆形（品牌色
  描边淡底），hover 实心蓝+光晕+抬升；置顶按钮悬浮钉子改白、未悬浮
  置顶态品牌色常亮；空状态大按钮保留。

## 便签快捷键统一到工具箱 + 全局注册 + 按钮样式（commit caa0608）
- ①快捷键设置：ShortcutPage 新增「便签」分组（7 项，支持纯 F 键录制），
  存 sticky_settings.json shortcuts；StickyNotePage 移除快捷键区改提示。
- ②全局快捷键真注册（修复失灵）：register_shortcuts stub→真实，用
  tauri_plugin_global_shortcut 注册 show_app/close_all/new_note，lib.rs
  with_handler 先短路 sticky 分发（show=show_all_open / close=play-close-anim
  广播 / new=quick_new_note），setup 启动注册 + 保存后重注册。
- ③新建按钮恢复胶囊样式放标题栏右侧（用户：换位置不改样式）。
- ④标题栏 icon-btn 统一圆形（品牌色描边淡底，hover 实心+光晕）。
- 注意：with_handler 闭包内 app 是具体 &AppHandle（Wry），可直调具体类型函数。
- 验证：tsc + vite + cargo build（exe 14:35）通过；已 commit 未 push。

## 新建按钮标题栏居中 + 呼出历史快捷键（commit 88f1c5c）
- ①新建便签按钮：胶囊样式保留，absolute 居中于标题栏（left:50%+
  translate(-50%,-50%)），hover/active transform 需叠加居中位移防跳动。
- ②快捷键分组新增 open_history（呼出历史便签面板，全局）→ 后端
  register/handle 数组加 open_history → open_history_window。

## 三处便签修复（commit，15:02 构建通过）
- ①新建按钮重叠：根因是 .new-note-btn 嵌在 .titlebar-right(position:relative)
  内，absolute left:50% 相对右侧窄容器定位→压住关闭；改为 .titlebar 直接子元素
  + z-index:4 修复居中。
- ②保存按钮常亮：改用 stickySaved 记录已存值，disabled 加『未改动』判断。
- ③呼出错弹历史：show_all_open 去掉 open_history 回退，仅操作 note_* 窗口
  （可见→收起/隐藏→show/全无→open_notes 重建→否则新建）；历史由 open_history 负责。
- 教训：absolute 居中元素必须放在 relative 祖先的直接子级，否则相对最近
  positioned 祖先定位导致偏移。

## 新建便签按钮失灵（commit a30968b）
- 根因：标题栏 mousedown→startDragging(原生 Tauri 拖拽) 只排除 .icon-btn，
  新建按钮(.new-note-btn) 未排除，点击冒泡触发原生拖拽后 click 被吞。
- 修复：拖拽排除名单加 .new-note-btn。教训：标题栏内所有可点击按钮都要
  在拖拽处理中排除，否则原生 drag 吞 click。

## 便签五项修复（commit a619e91，15:58 构建通过）
- ①移除「全部关闭便签」快捷键配置（含默认 Ctrl+Shift+X 与后端注册/分发）。
- ②修复「呼出便签无法关闭」：根因 WebviewWindow::is_visible() 对透明/置顶无边框
  窗口不可靠（show 后仍 false）→ toggle 永远走显示分支。改用运行时
  VISIBLE_NOTES（LazyLock<Mutex<HashSet>>）跟踪可见便签 label，show_all_open 与
  toggle_sticky_notes 统一基于该集合 toggle；× 关闭/销毁时清理。
  教训：透明/always_on_top/decorations(false) 窗口勿用 is_visible() 做状态判断。
- ③open_history 快捷键改为 toggle（HISTORY_VISIBLE 标志），与端口工具栏一致。
- ④便签标题栏抓取区：绝对居中→flex 中间占位（flex:1），窄窗不再与右按钮重叠；
  .crowded 隐藏历史/新建/最大化。
- ⑤便签右下角 .win-resizer（24px + 对角 grip 图标），mousedown→startResizeDragging('SouthEast')。
- 验证：tsc + vite + cargo build 通过；已 commit 未 push。

## 加宽便签恢复按钮 + 快捷键关闭播特效（commit 76b80d7）
- ①【加宽后收起按钮不恢复】根因：adaptTitlebar 用 `titlebar.clientWidth < 380`
  硬门槛——宽到 380 以下强制收起 3 按钮；且自适应只在 RAF + onResized 触发，
  原生缩放拖拽(startResizeDragging) 不触发 onResized → 加宽后不重算。
  修复：改纯内容溢出判定（去掉 380 门槛）；新增 ResizeObserver 监听
  document.documentElement（Webview 与窗口同尺寸，任何缩放都触发），兜底重算。
  注意：toggle .crowded 改的是标题栏内部布局、不改变窗口尺寸，ResizeObserver
  不会自循环。
- ②【快捷键关闭无特效】根因：show_app(show_all_open) 与工具栏便签入口
  (toggle_sticky_notes) 收起分支直接 w.hide()，从未 emit 本就存在的
  play-close-anim 事件（该事件仅前端 × 按钮/旧"全部关闭"用）。修复：收起时
  对每个 VISIBLE_NOTES 内便签窗口 emit play-close-anim → 前端聚焦窗口播粒子
  消散、其余立即收尾，动画结束 finishClose→close_window 自行清理 VISIBLE_NOTES
  + 广播状态；不再此处 hide()/clear()。透明主题无粒子动画属预期。
- 教训：原生 resize 拖拽不保证发 onResized，尺寸自适应要 ResizeObserver 兜底；
  全局快捷键的收起语义应与 × 按钮一致（走关闭动画事件）。
- 验证：tsc + vite build + cargo check 均通过；已 commit 未 push。
