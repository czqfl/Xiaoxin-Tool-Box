# 2026-08-20 工作日志

## 深色主题去蓝偏
- 主人反馈：工具箱深色模式整体偏蓝，希望改成 WorkBuddy 那种中性灰（20,20,20 / 31,31,31，R=G=B 无蓝偏）。
- 根因：深色主题背景令牌 B 通道高于 R（如 bg-app #16181d=22,24,29；panel 30,32,38）。
- 改动 `src/styles/theme.css`：
  - bg-app #141414(20,20,20)、bg-panel rgba(31,31,31)、bg-panel-rgb 31,31,31、bg-elevated (38,38,38)、bg-input (43,43,43)、bg-input-glass rgba(31,31,31)
  - `:root[data-theme="dark"]` 与 `@media (prefers-color-scheme:dark)` 两块同步改
  - text-inverse #16181d → #161616（中性）
  - **保留**品牌蓝(#4c9eff)、工具栏彩色图标、水彩渐变等小面积交互色（避免重蹈"死人界面"覆辙）
- 便签 styles.css 独立样式，按主人要求排除，未动。
- 提交：bb54ed3（仅 theme.css，工作树此前已干净，上一轮工具栏/setTheme 改动已先行提交）。

## 经验
- 深色"偏蓝"通常来自背景令牌 B>R 的细微色偏，而非 accent 色；去蓝只需中性化背景大色块，交互强调色保留。

## 便签背景预览布局修复
- StickyNotePage.tsx 「背景图片」行：SettingRow 右侧 .setting-control 是横向 flex，按钮行与预览 div 被当同排兄弟 → 预览在按钮右侧。
- 改用 flexDirection:column 容器包裹按钮行+预览，固定宽 360px(max-width:100%)，预览落到按钮正下方。提交 559e3c9。

## 工具栏「靠边自动收起」消失 bug 修复
- 症状：开启靠边收起后工具栏消失，重启也找不到。
- 根因：收起后仅露 4px 细边，恢复弹出只靠 onMouseEnter 命中该细边（几乎不可达）；且 expandFromEdge 的 byHover 自动再收起在静止光标下会弹出来又秒收，彻底消失。重启后 collapsedRef 内存重置本应展开，但默认位置若在边缘，鼠标离开又收起成找不到的细边。
- 修复 `src/modules/toolbar/Toolbar.tsx`（纯前端，Rust toolbar_geometry 物理像素坐标经核查无误）：
  1. 新增「靠近屏幕边缘自动弹出」：轮询检测光标靠近停靠边(EDGE_REVEAL=70px)即 expandFromEdge()，不再依赖戳 4px 细边。
  2. 移除 byHover 自动再收起，再收起统一交给 mouseleave + 轮询兜底（消除秒收/消失）。
  3. 启动兜底：窗口完全在显示器外时夹回屏内。
- 提交：aadef78。
- 经验：dock 类 auto-hide 的恢复触发半径(EDGE_REVEAL)必须小于窗口尺寸(win_w)，否则「离开窗口」(>win_w 距边)与「靠近弹出」(≤阈值距边)冲突会抖动；本例 win_w≥38 > 70? 实际 1 工具时 38<70 仍安全（离开需 >38，阈值 70 仅扩大"贴边常驻"区，无抖动）。

## 启动强制工具栏右下角（待 commit）
- 用户：悬浮工具栏经常「启动后直接不见了」，要求固定每次启动都出现在屏幕右下角。
- 根因：启动走 toolbar_set_visible(true) → restore_position() 恢复「记忆位置」
  （可能落到屏外/边缘），否则 position_toolbar_default 用「光标所在屏幕右边缘
  中部」——两者都不确定；记忆位置若曾被收起/隐藏存成屏外坐标，启动即不可见。
- 修复：新增 show_toolbar_at_bottom_right()（panel.rs），启动直接把工具栏
  set_position 到主显示器右下角（距边 16px，逻辑像素）再 show，不走 restore。
  lib.rs 启动流程改调它（替换 toolbar_set_visible(true)）。toggle/设置开关仍走
  restore（保留关闭后回到上次位置的习惯）。
- 注意：auto_hide 默认开，启动鼠标不在工具条上不会立刻收起（收起只在 onMouseLeave
  触发）；若用户不想要「靠近边缘自动收起」，在 悬浮工具栏 设置关掉「靠边自动收起」。
- 验证：cargo check 通过。待用户 tauri dev 重启实测启动位置。

## 贴边收起「整条飞出屏幕 / 靠近不弹出」——高分屏缩放坐标单位错位（Req G，待 commit）
- 症状（主人 8/19 反馈）：一启动靠边收起，工具栏直接收没了（没有露出 4px 边框），
  且鼠标靠近屏幕边缘也不弹出。强调「昨天在另一台电脑正常」→ 不是分辨率问题，是 DPI 缩放。
- 根因：toolbar_geometry（Rust）返回的 cursor/win/monitor 坐标**全是物理像素**；
  而 animateWindowTo() 与启动兜底 setPosition 用的是 LogicalPosition —— Tauri 会把
  LogicalPosition 再乘一次 scale factor。100% 缩放时物理==逻辑不出错（所以另一台电脑正常）；
  125%/150% 缩放下被二次放大，工具栏被推到屏幕外数千像素（整条消失、无细边），
  expandFromEdge 的目标坐标也错乱 → 靠近边缘永远触发不了弹出。
- 修复 `src/modules/toolbar/Toolbar.tsx`（纯前端）：
  1. import 改 `{ LogicalSize, PhysicalPosition }`（剔除不再需要的 LogicalPosition）。
  2. animateWindowTo 内 `setPosition(new LogicalPosition(x,y))` → `PhysicalPosition(x,y)`。
  3. 启动兜底（窗口完全在屏外夹回）`setPosition(new LogicalPosition(...))` → `PhysicalPosition(...)`。
  4. probe() 的 inside/cursorNearEdge/detectEdge 全程用 toolbar_geometry 物理像素，单位一致，无需改。
- 验证：npx tsc --noEmit 通过（noUnusedLocals 开启，导入已清理）；cargo check 命中缓存通过。
- 经验：Tauri 里 outer_position()/outer_size()/cursor_position()/toolbar_geometry 返回的
  **物理像素**，凡是把它们再喂给 setPosition/setSize 时，必须用 PhysicalPosition/PhysicalSize，
  绝不能用 LogicalPosition（除非你明确先除以 scale factor）。高分屏 double-scaling 在低缩放比
  下完全隐形，是最隐蔽的一类 bug。

## 发布：README + 版本 1.0.1 + npm run pack 打包 + git push（进行中）
- 主人要求：写应用功能说明 md、此次 git push、打包安装程序并上传。
- 文档：根目录新增「应用功能说明.md」→ 主人点名要 README → 改名 `README.md`
  （内容含功能总览/悬浮工具栏/各模块/主题/快捷键/设置/安装运行/版本更新）。
- 版本：Cargo.toml、tauri.conf.json、package.json 三处 1.0.0 → 1.0.1
  （本次含 Req F 启动右下角 + Req G 高分屏贴边收起修复）。
- 打包：`npm run pack`（scripts/build-release.ps1）：tauri build（NSIS）→ 复制为
  根目录「小心工具箱-安装版-v1.0.1.exe」，并打「小心工具箱-便携版-v1.0.1.zip」（绿色版，数据存 data\）。
- 主人 8/20 反馈「工具栏又不见了」：其运行的是修复前的 v1.0.0 二进制（8/19 14:10 打的包），
  启动右下角/DPI 修复已在代码并提交，但未打进二进制 → 需等新包；lib.rs:231-233 启动链路已核验无误。
- 推送：git push origin main（主人本次明确要求 push，此前是只 commit 不 push）。

## 收起细边「太少且不显眼」→ 加宽 + 品牌色亮轨（SLIVER 4→12）
- 主人 8/20 反馈：收起时露出的边框太少、太不显眼（说明 Req G 修复已生效，能正常留边了）。
- 改动：
  1. `Toolbar.tsx` SLIVER 4 → 12（露出 12px，够宽够显眼）。
  2. 收起时按停靠边加 `edge-${edge}` 类；弹出时四边类全清。
  3. `toolbar.css`：`.toolbar.edge-collapsed` 改为品牌色亮轨
     （edge-left/right/top/bottom 对应 `border: 8px solid var(--accent)`）+ 呼吸柔光
     （`toolbar-edge-breathe` 2.6s 动画，基于 --accent-rgb）。
- 验证：tsc 通过。提交（未 push，主人默认只 commit）。

## 弹出瞬间「右侧突然变蓝」→ 亮轨类改在动画开始前移除
- 主人 8/20 反馈：工具栏弹出时右侧突然变蓝，然后恢复正常。
- 根因：`expandFromEdge` 原先**动画播完才移除** `edge-collapsed`/`edge-*` 类，
  滑入的 160ms 里 8px 品牌色亮轨（贴右收起=右侧蓝边）一直挂在窗口上。
- 修复：classList.remove 移到 `expandFromEdge` 开头（snappingRef 置位后立即执行），
  亮轨只在完全收起态存在；收起时仍是动画结束后才加（对称）。
- 验证：tsc 通过。提交（未 push）。

## 便签呼出「先清晰后模糊」→ 首次开启磨砂直接落定
- 主人 8/20 反馈：便签呼出时先出现正常背景，然后才模糊；要求一出现就是模糊效果。
- 根因：applyGlassBlur 首次开启（元素无 .glass）时先置 --glass-blur:0px 再加类，
  再 tweenGlassBlur 用 280ms 从 0 缓动到目标半径 → 第一帧清晰、随后逐渐变糊。
- 修复 `src/sticky/glass.ts`：首次开启直接 `--glass-blur: <目标px>` 落定 + 加 .glass，不再渐变；
  已在磨砂态下改强度/开关仍走 rAF 平滑过渡（滑块/开关不跳变）。历史窗口、设置弹窗同受益。
- 验证：tsc 通过。提交（未 push）。

## 便签呼出「闪一下」→ 显示时机后置到视觉效果就绪之后
- 主人 8/20 反馈（接上一条）：改完呼出即磨砂后，便签呼出时会闪一下。
- 两个闪帧来源，均已修：
  1. 前端 note.ts init：原先先 getCurrentWindow().show() 再 applyBackground/applyGlassEnabled
     ——第一帧以默认外观画出。已把 show/hide 块挪到 applyMdMode/applyTheme/applyBackground/
     applyGlassEnabled 全部执行完之后；show 后补 setFocus（接替后端原来对新窗的 set_focus）。
  2. 后端 sticky.rs ensure_note_window 新建分支：build() 后立即 win.show()+set_focus()，
     此时 WebView 尚未加载完 → 空白/默认外观闪帧。已移除，新建窗口改由前端 init 显示
     （各呼出路径都先 mark_note_open_inner，前端 getOpenNotes 命中即 show）；
     "已存在"分支仍即时 show（视觉效果早已应用）。广播/VISIBLE_NOTES 登记保留。
- 验证：tsc 通过；Rust 改动待 v1.0.2 构建编译验证。提交（未 push）。

## 快捷键两次才呼出 + 打包任务死掉 + ThemeMode 缺 skyblue/red
- 主人 8/20 反馈：便签现在要连续按两次快捷键才能呼出。
- 诊断（diag.log 实锤）：第一次按键 "built (frontend shows after visuals ready)" 后前端
  没显示；第二次按键才走 "shown existing"（后端已存在分支 show）。
  → init 里 show 之前的某步（外观应用链）异常/挂起，导致显示代码不执行。
- 修复：
  1. note.ts init：外观应用（applyTheme/applyMdTheme/applyBackground/applyGlassEnabled 等）
     整段包 try/catch，失败也继续走到显示逻辑——显示优先级高于上妆；
     显示决策处加 diag_log（输出 noteId + open 数组，便于日后定位）。
  2. sticky.rs ensure_note_window 新建分支：加 500ms 兜底强制 show+set_focus
     （正常路径前端 ~300ms 已显示，兜底 no-op），保证一次呼出必现。
  3. 顺带修复：Rust config.rs ThemeMode 缺 Skyblue/Red 变体 → 设置里选浅蓝/红色主题
     config_save 一直报 unknown variant（diag 里大量 invoke error）；
     已补变体并更新 lib.rs window_theme_is_light 匹配（skyblue/red 按浅色）。
- 教训：npm run pack（powershell 包装脚本）在后台挂死 41 分钟零产出且无进程——该脚本
  不适合本环境后台跑；改用直接分步构建：npm run build → npx tauri build → 手动复制
  NSIS 产物到根目录 + 打便携 zip（等价 pack 脚本产物）。

## v1.0.1 打包成功（直接分步构建）
- 路径：rm -rf dist 预清理（绕过 safe-delete shim 对 vite emptyDir 的回收站拦截）
  + CODEBUDDY_SESSION_ID/CLAUDE_SESSION_ID 置空 + npx tauri build（前台可见输出）。
- 首次 npx tauri build 失败：sticky.rs 兜底线程 E0505（闭包 move 与调用借用冲突）→
  闭包内再 clone 修复，commit 67ecb21；cargo check 通过后重跑成功（4m51s）。
- 产物（根目录）：
  小心工具箱-安装版-v1.0.1.exe（NSIS，2.3MB）
  小心工具箱-便携版-v1.0.1.zip（绿色版，2.7MB，data\ 存数据可迁移）
  Cargo.lock 因版本号 1.0.1 更新 → 已提交。
- 安装包未纳入 git（与 v1.0.0 一致，仅本地分发）。
- 下一步：主人安装 v1.0.1 实测「快捷键一次呼出」与工具栏/便签各修复。


## 历史便签面板「打不开便签」修复
- 主人反馈：有时候通过历史便签面板点不开某个便签。
- 诊断（diag.log 实锤 + 代码）：
  1. 历史面板本身是 always_on_top 且持有焦点；从面板点开便签时后端 open_note_window
     只 show+set_focus，便签常被历史面板压在后面（Windows 前台锁令 set_focus 偶发不生效，
     代码 note.ts:904 注释自承此点），表现为"点了没反应/打不开"。
  2. diag 还显示便签窗口偶发被销毁需重建（无 hide 日志）——疑似 WebView2 渲染崩溃
     或关闭动画裁切态复原竞态；属另一类隐患，本次先不深改。
- 修复 `src/sticky/history.ts`：卡片点击 openNoteWindow 之后、以及新建便签 createNewNote
  之后，均 closeWindow() 收起历史面板（隐藏常驻，可再开），保证便签一定露在最上层。
- 提交：67075e0（仅 commit 不 push，主人默认）。
- 待观察：若仍"打不开"但便签已弹出（空白/需多次），则是关闭动画 blanked 复原竞态或崩溃，
  再深入 note.ts summoned / 动画 cleanup 与 anim.load() WebGL 崩溃排查。

## 历史面板"打开便签就关面板"回退 + 关闭延迟修复
- 主人反馈：上一条"打开便签收起面板"不对——面板不该关闭；且快捷键关闭便签慢(1~2s)。
- 回退 history.ts 的 closeWindow 收起逻辑（面板保持常开）。
- 遮挡修复改到 Rust 端：`ensure_note_window` 已存在窗口分支在 set_focus 后补
  `win.set_always_on_top(true)`，把便签重新插到置顶层最上方，压过历史面板且不关面板。
- 关闭延迟根因：默认「粒子光效」关闭动画 runGlowDissolve 总时长 ~1000ms（还有 4000ms
  看门狗兜底），透明主题本应走 note.ts:1847 立即 finishClose 无延迟；但透明判定依赖
  bg-transparent CSS 类，applyBackground 初始化抛错时类没加上 → 误走动画分支白等 ~1s。
- 修复 note.ts requestAnimatedClose：改为 async，透明判定以 getSettings().theme 为权威
  （读不到设置才回退 CSS 类），透明主题必定即时关闭、无动画无延迟；非透明仍走动画分支。
- tsc 通过；Rust 改动未 cargo 编译验证（仅加一行 set_always_on_top）。提交 e817c2f（不推送）。
- 待观察：若主人在非透明主题仍"看不到动画只有延迟"，是粒子层 canvas 渲染问题，需另查。

## Bug3：历史面板透明主题"是张图片而非实时模糊" + 主题设置似不生效
- 主人反馈两点：①透明主题下历史面板背景是张静态壁纸图，不是实时模糊；②便签主题设置改完没效果。
- 根因（①实锤）：
  - 历史窗口 `open_history_window` 创建时是 `.transparent(false)` → WebView 默认不透明；
    透明主题只能退化成「读桌面壁纸→压成 1920 JPEG→当 --note-bg-img 静态背景」冒充模糊
    （history.ts applyHistoryBg 调 applyPanelBackground 注入；panel-bg.ts getWallpaperDataUrl）。
  - 便签/设置窗口都是 transparent(true) + setAcrylic 原生亚克力实时模糊，路径本就通；历史窗口
    因当年"规避透明 WebView2 挂起"注释被设成非透明，导致只能塞静态图——注释已过时
    （设置窗口 transparent(true) 实时亚克力一直正常）。
  - ②"主题设置不生效"实为同一透明观感问题：用户看到的历史面板（高频可见）是冻结壁纸图，
    便签窗口本身透明实时模糊正常（note.ts init + onSettingsChanged 都已正确 applyBackground）。
- 修复（三处，commit e7d4c09，不推送）：
  1. `src-tauri/src/sticky.rs` open_history_window：`.transparent(false)` → `.transparent(true)`
     （保留 apply_panel_effects_for(&w,false) 仅做 DWM 圆角）。
  2. `src/sticky/history.ts` applyHistoryBg 透明分支：不再调 applyPanelBackground（去静态壁纸），
     改为镜像 note.ts applyAcrylic——加 bg-transparent 类 + 写 --trans-opacity(0.6×滑块,上限60%)
     + setAcrylic(true,1,tint) 实时模糊；<2% 走 transparent-clear 完全透明。导入 normalizeOpacity/parseColorToRgbInt。
  3. `src/sticky/styles.css` .history-window.bg-transparent：纯 transparent →
     color-mix(var(--bg) calc(--trans-opacity*1%) transparent) 主题色面板 + 内嵌光边；补 .transparent-clear 去边框。
- 验证：npx tsc --noEmit 通过。修复后透明主题下历史面板与便签一致走 DWM 实时磨砂。
- 经验：Tauri 透明主题观感前提 = 窗口 transparent(true)（WebView 默认透明）→ DWM 亚克力才能透出；
  任何"塞静态图冒充模糊"都是该窗口被设成 transparent(false) 的退化方案，应优先改为透明窗体而非贴图。

## Bug4：便签打开状态异常 + 历史面板点击卡死
- 主人反馈两点：
  ①便签首次打开后，再点历史列表该便签显示"打开中"但内容没渲染出来；
  ②历史面板点击操作容易无响应/卡死，要关掉重开才能继续。
- 根因：
  ①`open_note_window` 只给便签窗口发一次 summoned；新建窗口经 defer_to_main_loop 延迟构建，
    命令执行时窗口还不存在 → summoned 丢失 → 便签被 show 出来却空白/不复原。
    另 `minimize_to_tray` 隐藏便签只移 VISIBLE_NOTES、不移"打开中"持久集合 → 隐藏却显示打开中。
  ②历史面板渲染三路并发（sticky://state-changed 事件 + 聚焦 + 500ms 轮询）都直接调 render()，
    整表重建竞态：用户点击瞬间重建把卡片换掉 → 点击被吞/误触；render 无 try/catch，异常堆积像卡死。
- 修复（commit 7421294，不推送）：
  1. sticky.rs `open_note_window`：分时补发 summoned（立即 + 120ms + 600ms，线程 sleep + run_on_main_thread
     emit），另兜底 show+unminimize+set_always_on_top；确保任意时机 JS 就绪都能收到并复原内容。
  2. sticky.rs `minimize_to_tray`：隐藏便签时 mark_note_closed_inner 同步移除打开集合 + 广播，
     托盘隐藏的便签历史显示"已关闭"，与实际一致（main 特殊便签除外，与原版一致不持久化）。
  3. history.ts：三路刷新统一收口 `requestRender`（renderPending 去重 + setTimeout(0) 调度，
     不用 rAF——窗口隐藏时 rAF 不触发会让 pending 卡死）；pointerdown 期间延后重建、pointerup/cancel
     再排一次；卡片点击改容器事件委托（listEl 单监听 + data-id，重建不丢点击）；render 整体 try/catch。
- 验证：npx tsc --noEmit 通过；cargo check 通过（先有 label2/label4 未使用警告，已清理）。
- 经验：
  - 后端向"可能正在构建/尚未挂载监听器"的 WebView 发事件，必须分时补发，首帧必丢。
  - 列表类 UI 的高频重建 + 每项独立监听 = 点击竞态重灾区；容器事件委托 + 渲染去重 + 交互期冻结重建，
    是根治"点了没反应/卡死"的三件套。
  - 窗口隐藏态不要用 requestAnimationFrame 做状态调度（rAF 在不可见页面不回调，flag 会卡死）。

## Bug5：重写便签打开/刷新/主题联动逻辑（用户要求删无用代码重做）
- 主人反馈：仍不行 + "设置里改主题，历史/便签面板不实时更新" + "删去无用代码，重新实现一套逻辑"。
- 三个确定性根因：
  ①历史面板裸 listen("settings-changed") + getSettings()：settings.ts 的 getSettings() 返回本窗口
    模块缓存的 cached，只有 onSettingsChanged→notifyChanged 才会从磁盘重载缓存；裸监听永远拿旧值
    → 改主题历史面板不更新。**必须统一走 onSettingsChanged。**
  ②applyHistoryBg 只更新背景、不同步 theme-dark 类 → 深色/浅色切换配色不实时。
  ③note.ts summoned 处理器：透明主题分支提前 return，跳过了 setFocus 与强制重绘——WebView2 透明
    窗口 hidden→shown 后偶发不复绘，正是"显示打开中但内容空白"的直接原因。
- 重构（commit 待定，不推送）：
  - history.ts：删 500ms 轮询（stopPoll/startPoll）与 diag 日志；三路刷新只剩「事件 + 聚焦兜底」；
    裸 listen 改 onSettingsChanged；applyHistoryBg 开头同步 theme-dark 类。
  - note.ts：summoned 删 blanked 检测，改为无条件清 clip/mask/opacity/boxShadow + 强制重绘（透明分支
    不再 return）；呼出时重应用 applyTheme/applyBackground/applyGlassEnabled（隐藏期间设置被改的兜底）；
    finishClose 与 btnTray 不再保留"空画面"裁切态（finishClose 先 hide 再清样式）。
  - sticky.rs：open_note_window 删上一轮加的 summoned 多线程补发——已存在窗口一次 emit 必达，
    新建窗口靠前端 init 依"打开集合"自显；恢复为 ensure + show/unminimize/置顶 + 一次 summoned。
- 验证：tsc + cargo check 通过。
- 新逻辑（单一事实来源 + 事件驱动 + 幂等）：打开集合=唯一状态源；状态变更后端必广播；便签本体
  永不留裁切残留，呼出=无条件复原+重绘；设置刷新统一 onSettingsChanged（先重载缓存再回调）。

## 便签功能彻底移除（commit 28dbce5，不推送）
- 主人最终决定：放弃再修便签，要求**完全移除**整个 sticky notes 功能（"算了，完全移除这个便签吧"）。
- 删除范围：
  - 前端整套 `src/sticky/` 目录（26 个文件：note/panel/settings/history/particles/glass/flame/summon 等）
  - `src/settings/StickyNotePage.tsx`、工具栏"便签"入口(IconSticky、TOOLS.sticky、PANEL_LABEL_TO_KEY 的 sticky-history)、设置页 NAV 便签设置项、ShortcutPage 便签分组
  - Rust 后端 `src-tauri/src/sticky.rs`（~1760 行，37 命令、数据迁移、全局快捷键、粒子层窗口）
  - `data/sticky_open_notes.json`、`tauri.conf.json` 的 `particles` 窗口声明
  - `acrylic.rs` 仅便签用的 `apply_acrylic_tinted`、`theme.css` 6 处 `--tool-sticky` 变量、`settings.css` 便签样式块
- 安全迁移（被其他模块依赖，不能随 sticky.rs 消失）：
  - `defer_to_main_loop` 从 sticky.rs 迁到 `lib.rs` 作为公共助手（panel.rs 依赖，避免同步 IPC 回调内重入 build WebView2 窗口挂死）
  - `save_settings/load_settings/register_shortcuts` 唯一非 sticky 调用点是 StickyNotePage/ShortcutPage 便签分组，二者均删，故整体删 sticky.rs 不破坏工具箱自身快捷键
- 验证：前端 `npx tsc --noEmit` 退出码 0；后端 `cargo check` 退出码 0 且无警告。
- 提交：28dbce5（40 files changed, 21 insertions(+), 16504 deletions(-)）。按习惯只提交不推送。
- 经验：彻底删除一个大功能时，先 grep 全仓确认每个被删符号的唯一调用点都在被删模块内（或随之一并删除），再决定能否整体移除；被其他模块依赖的通用助手需上迁而非随删。

## 工具栏空位 bug（便签残留未清，commit 98aa5ff）
- 症状：悬浮工具栏末尾（竖排即底部）多出一个空位。
- 根因：上次移除便签只动了代码（TOOLS 删 sticky），**本地持久化配置 `data/config.json` 的 toolbar.tools 仍含 "sticky"**。
  代码里 TOOLS[sticky] 已不存在 → 渲染 `if (!tool) return null` 跳过、不撑宽；但窗口尺寸按 `tools.length` 算仍按 7 格 → 窗口比按钮宽一格 = 空位。
- 修复：
  1. `data/config.json` 删 toolbar.tools 的 "sticky"。
  2. `Toolbar.tsx` 尺寸/渲染守卫改用 `validTools = tools.filter(k => TOOLS[k])`，未知键不再撑宽，杜绝复发。
  3. 删孤儿数据 `data/sticky_notes/`（已移除功能、无法再打开）。
- 验证：tsc --noEmit 通过。
- 经验：删功能别只动代码，**运行时持久化配置/数据也要一并清理**；且配置驱动 UI 时，尺寸计算应基于"有效键"而非"原始配置键数"，对历史脏配置更鲁棒。
