# 2026-08-30

## OCR 高亮与状态标识（贴图 + 截图）

- 贴图文字选择模式：在 `PinWindow.tsx` 新增左上角常驻标识（class `pin-textmode-badge`，CSS 见 `pin.css`），当 `ocr.altActive` 为真时显示「文字选择模式 · 拖动已锁定 · 再按 Alt 退出」，避免用户困惑为何无法拖动贴图。
- 截图 OCR 文字高亮：`ScreenshotOverlay.tsx` 在 `ocrPhase==="done"` 后于截图上渲染半透明蓝块覆盖每行识别文字（class `shot-ocr-hl`，CSS 见 `screenshot.css`，z-index 9、pointer-events:none）。坐标映射：OCR 行是裁剪图物理像素，÷`cssScale()` 归 CSS 像素 + 选区左上角 `region`，与冻结帧对齐。
- 说明：截图高亮仅展示用（复制仍走右侧 OCR 面板划选/Ctrl+C），未做图上交互式划选，与贴图 Alt 模式视觉一致但非全功能复刻。
- 类型检查 `tsc --noEmit` 通过，已本地 commit（未推送）。

## OCR 交互统一（贴图 ↔ 截图）—— 6 项需求闭环

- 提取共享弹窗 `src/modules/shared/OcrPanel.tsx` + `ocr-panel.css`：贴图与截图共用同一「文字识别」面板（标题/复制全部/复制译文/翻译/返回原文/关闭 + 原文·译文两列 + 划选提示条），纯展示+回调，不持有状态；样式带 `var(--accent-rgb, …)` 主题兜底（贴图窗不加载 theme.css 时也正确着色）。
- 统一触发：两种状态均支持 Alt 启动 OCR（截图 `ocrPhaseRef` 守卫 idle/error→runOcr、done→切换文字模式；贴图 `usePinOcrSelect` 的 Alt keydown）。未贴图右下角保留「文字识别 (OCR)」按钮作为额外入口。
- 未贴图文字划选冲突修复：`onDown` 在 `ocrPhase!=="idle"` 时拦截左键、禁用「新建接收区域」，改走 `beginScreenshotTextSelect`（÷cssScale+region 坐标映射，行级吸附+词级命中，rAF 合帧）；`textSelectingRef` 防止与 React onMove/onUp 选区拖拽冲突。
- 贴图弹窗统一：`PinWindow.tsx` 在 `ocr.altActive` 时渲染 `<OcrPanel>`（停靠右上角，stopPropagation 不触发拖拽/划选）；`pinOcrTranslatingRef` 守卫 `EVT_TRANSLATE_LINE` 回填，防与截图窗串扰。
- 左上角标识优化 + 非贴图补齐：`shot-textmode-badge`（含 `::before` 蓝色脉冲状态点）与 `pin-textmode-badge` 视觉一致；非贴图新增 `shot-ocr-busy`（左上角 loading 脉冲 + spinner）与 `shot-ocr-sel`（划选词蓝块）。
- 修复 tsc 报错 3 处：`OcrPanel` 删除未用 `transMode`；`ScreenshotOverlay` 补 `ocrPhaseRef`（useRef+useEffect 同步）。`tsc --noEmit` 通过、`vite build` 通过（1947 模块）。
- 未推送，待本地 commit。
