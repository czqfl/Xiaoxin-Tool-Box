# 2026-09-02 工作日志

## 全局命令面板重写 + 本机应用扫描补全

### 1. 命令面板 UI 重写（主人反馈：样式单调 / 信息少要滚动 / 颜色没区分度）
- `CommandPalette.tsx` 重写为**左分区导航 + 右双行结果**：
  - 左栏按来源（kind）分区，带全量命中计数（`counts` 来自 `QueryResult`，不受展示限流缩水）；
  - 点击或 **Tab / Shift+Tab** 循环切换分区；Esc 语义改为「先清分区再关窗」；
  - 行改双行（标题 + 副标题），行尾 tag / 热键 / Ctrl+Enter 提示；
  - 每个来源一个主题色（`KIND_COLOR`，走 `--tool-*` token，`color-mix` 派生底色描边），
    图标座 / 分区头 / 左栏点 / 色条统一用该色；
  - 分区头吸顶（`position:sticky`，实底 `--bg-elevated`，勿用 backdrop-filter）；
  - PageUp/PageDown/Home/End 翻页；窗口 680×480 → **780×600**（tauri.conf.json）。
- `commands.ts`：`queryItems/emptyStateItems` 返回 `QueryResult{items,counts}`；
  分区限流 PER_GROUP=10、TOTAL=80，聚焦分区放宽到 FOCUS_GROUP=40；
  换词才 120ms 防抖（`lastQueryRef` 区分），仅换分区 0ms 重算；
  **空态常驻「本机应用」前 30 条**（EMPTY_APPS）——此前空态完全没有应用，
  是"扫不到应用"观感的一部分。
- `types.ts`：新增 `KIND_ORDER` / `KIND_COLOR`。

### 2. 本机应用扫描补全（主人反馈：微信、企业微信扫不到）
- 旧版只有 开始菜单.lnk + App Paths 两个来源；新增：
  - **卸载表注册表**（HKLM/HKCU × WOW64 32/64，DisplayName + DisplayIcon/InstallLocation），
    微信/企业微信这类用户级安装的唯一可靠登记处；过滤 SystemComponent/补丁/KB；
  - **常见安装目录扫描**（Program Files ×3 + LOCALAPPDATA\Programs + LOCALAPPDATA，
    下钻 2 层，只认"主 exe 与所在目录同名（或父目录为版本号+祖父同名）"，
    4000 目录 / 2500ms 预算兜底）。
- 拆分：`apps.rs`（命令+内存/磁盘缓存）+ `apps_scan.rs`（纯扫描，无 crate 依赖）。
- junk 修正：**短关键字（≤6 字符）只在"显示名/文件名"上匹配**，不再对整条路径 contains
  （否则 `cmd/calc/store` 这类片段在长路径里误伤）；补 `upgrader/clicktorun/sdxhelper/
  skypeserver/msoxmled/msoadfsb/msoasb/vcpkg`（Office/套件辅助进程实测混入）。
- 启动菜单名优化：`微信/微信` → `微信`（目录名=快捷方式名时去重）。
- **真机验证**（临时 bin `src/bin/appscan.rs`，验证后已删）：
  开始菜单=111 卸载表=34 AppPaths=44 目录=19 → 去重 106 条；
  微信→`D:\...\Weixin\Weixin.exe`、企业微信→`WXWork.exe`、QQ 全部命中；
  耗时 1.8s（首次 14s 是冷缓存+双扫，含图标提取）。
- **启动预热**：lib.rs setup 里 `std::thread::Builder("app-scan")` 后台跑
  `apps::warm_from_disk`（读 `data/app_cache.json` → 过期(24h)才重扫 → 落盘）；
  `list_installed_apps` 命中缓存零等待返回，过期先交货后台重扫。
- 缓存文件：`storage.rs` 新增 `app_cache_file = data/app_cache.json`（含图标约 1MB）。

### 3. 上一轮遗留：便签名称输入加长
- `.title-input` max-width `min(58%, 264px)` → `min(100%, 320px)`（左列仅标题一个元素，
  抓取区是独立网格轨道不会被压；抓取点阵样式上一轮已移除，本轮确认无需再动）。

### 4. 验证
- `npx tsc --noEmit` ✓、`npx vite build` ✓、`cargo check` ✓（均无警告）。
- 全部文件 CRLF 一致（Write/Edit 工具本会话直落真实 D:，git diff 干净）。

### 环境坑（重要）
- **本会话 Write/Edit/Read 直落真实 D:**（探针文件 + git 均可见），旧记忆的"覆盖层"
  结论不再成立；但每次会话先放探针文件验证一次再选工具，最稳。
- **cargo test 在本 crate 不可用**：连空测试都在加载期崩（exit 0xc0000020，疑与
  ort/原生依赖测试链接有关）。要跑扫描类逻辑验证，用临时 `src/bin/xxx.rs` +
  `cargo run --bin xxx`（不链接 lib 主状态），验证完删除。
- PowerShell 工具会拦 COM 实例化（WScript.Shell 解析 .lnk 被拒）；解析 lnk 走 Rust
  IShellLink（apps_scan::resolve_lnk_target）。

提交：见 git log（palette 重写 + 应用扫描 + 便签输入加长）。

## 贴图右键菜单补刷亚克力（0914276）

- **现象**：贴图（pin）右键弹窗只有半透明底色，没有其他面板的亚克力模糊。
- **根因**：`pin-menu` 是前端 JS 动态创建的窗口（App.tsx 启动预建待命单例 +
  PinWindow 右键 emitTo 复用），**不在 `apply_panel_acrylic` 启动管线里**（那条只遍历
  tauri.conf.json 静态声明的 9 个面板标签）——所以从未被 apply_rounded_corners /
  apply_blur(SWCA) / make_webview_transparent 触达。
- **修复**（最小改动，仅 `PinMenu.tsx` +5 行）：菜单窗每次 `show()` 成功后
  `invoke("panel_refresh_acrylic", { label: "pin-menu" })`，与各面板 show→refresh 同序；
  命令内部读 `acrylic_enabled` 开关。useLayoutEffect 依赖 `data`（每次 emit 都是新对象），
  所以复用显示也必走补刷，顺带兜住 z-order 变化导致 SWCA 失效的老问题。
- **经验**：本项目「亚克力」统一入口 = `panel_refresh_acrylic(label)` 命令（读配置 +
  apply_panel_effects_for）。今后凡是【前端动态创建】的新弹窗，显示后都要 invoke 一次；
  窗口 CSS 半透明底（`rgba(--bg-panel-rgb, --panel-opacity)` 0.75）已就绪，无需改 CSS。
  验证仅前端改动时 tsc + vite build 即可（无 Rust 变更不用 cargo check）。

## 截图：二级选项残留复位 + 文字输入框升级（c5ff1f8）

- **二级选项残留**：遮罩窗复用（ScreenshotOverlay 组件跨会话不卸载），loadSession
  复位了一切却漏了 `submenuOpen` → 上一会话收场时开着的子选项面板下次呼出自动弹出、
  且无选中项（工具已复位 select）。修复：loadSession 批量复位处补 `setSubmenuOpen(null)`。
  经验：**遮罩窗复用架构下，新增任何「会话态」都必须进 loadSession 的复位批次**。
- **文字输入框升级**：旧样式是裸透明框（1.5px 白描边 + 2px 圆角），观感廉价。
  新样式 = 工具栏同族实底胶囊（#f5f5f7 / rgb(32,32,33) + 1px 细描边 + 12px 圆角 +
  同款投影），**底材随文字亮度反转且与主题无关**（`isLightColor`，sRGB 加权亮度
  阈值 0.62，落在黄/橙之间）：色板同时含 #ffffff/#000000，若跟随 --tb-bg 主题翻转，
  暗主题下黑字必落深底不可读——两套材质写死、`.light-text` 类切换。
- **不可破坏的契约**：`.shot-text-editor input` 的 `bold 16px/16px sans-serif` 行盒 +
  `textBaseline="top"` + commitText 量 input `getBoundingClientRect` 三者构成文字标注
  「所见即所得」定位链，改样式只能动容器，绝不能动 input 字体/行盒。
- **二次反馈定稿（36a5f60）**：主人不要胶囊实底——文字编辑器最终形态 = **透明底 +
  方角（无圆角）+ 1px 白线 × box-shadow 外圈 1px 深线的双线边框**（任意截图内容上
  都有轮廓）+ 极轻投影；**文字一律零 text-shadow**（重影根因，编辑态=canvas 成品）。
  底材亮度反转机制（isLightColor / .light-text）整体拆除。教训：主人审美是
  「轻、透、方」的 Snipaste 式编辑器，不要往实底胶囊方向发挥；预览可读性交给
  双线边框而不是给文字加阴影。

## 画笔粗细图标：四角框随圆贴合生长（e9d5c1d，已推送 GitHub）

- **需求**：粗细圆点变大会穿过四角取景框（固定 22px，max 时穿出约 4px）；
  主人要框「贴合圆变大、融为一体」。
- **几何根据**：SVG viewBox 24 中臂尖距中心 9.85 单位 = 半幅的 0.821——圆与臂尖
  相碰的临界在 sw≈12。公式 `frame = (D+1.5)/0.821`（D=圆径）：从临界起框同心外扩，
  臂尖与圆边保持 **~0.8px 恒定细缝**（环抱感），基态 22px 不变。
- **同步动画**：框 svg 与圆 span 用同参数 `width/height 80ms ease` 过渡，
  滚轮连续调节时二者互追、严格同步滑动。
- **坑**：框 max 33.5px > 30px 按钮，居中不能用 `inset:0+margin:auto`
  （溢出时 auto margin 归零、退化为非居中偏移）→ 改 `left/top 50% + translate(-50%,-50%)`。
- **约定更新**：主人指令改为「每次完成任务 commit + push 到 GitHub」，
  MEMORY.md 已同步修正；本次起 push 到 origin main（b053bfb..e9d5c1d 含此前 7 个提交）。
## 粗细图标去动画 + 按工具切换鼠标光标（本次，待提交）

- **删除弯曲动画**（fabd503 的 SWI 几何整体移除）：主人「算了，删除这个动画效果吧」。
  恢复静态四角框（22px / viewBox 24 / 4 角路径，与 e9d5c1d^ 完全一致）；
  圆径公式 `Math.min(4 + sw*0.6, 14)`——22px 框中心安全区 = 2×(22×8/24) ≈ 14.67px，
  封顶 14 留余量，圆恒在框内不触角臂（旧 4+1.2sw 封顶 26 会穿框）。
- **工具光标语义**：新增 TOOL_CURSOR 映射——绘制/框选=crosshair、文字=text(I 形)、
  select=default；root 光标 render 动态化（框选阶段恒 crosshair）；handlers 层
  pointermove 按 TOOL_CURSOR 起步，select 选区内→move、手柄→pointer，形状手柄/本体
  覆盖 pointer/move；tool 变化 effect 即时同步 handlers 光标（不等鼠标移动）。
- 验证：tsc ✓ vite build ✓ CRLF ✓。commit+push（注意 429 限流至 09-03 09:09 UTC+8，
  若 push 失败留本地）。
## 圆径线性映射修正 + 每工具独立 PNG 光标（6b3142b，已推送）

- **圆径映射**：上版 Math.min(4+sw*0.6, 14) 在 sw≥17 封顶，圆先到最大而粗细还能调
  → 改全程线性 `4 + (sw-1) * (10/23)`：sw1=4px → sw24=恰 14px（安全区 14.67 留
  余量），无封顶段，滚轮每档圆都在变。
- **工具光标差异化**：上版绘制类全映射 crosshair，图形/线段/画笔切工具肉眼无变化；
  且 **Chromium/WebView2 不支持 SVG cursor**（只认 PNG/CUR/ICO）→ 改运行时
  canvas 现绘 PNG：24×24、hotspot(12,12)、黑粗底(2.4)+白细芯(1.05)双层任意底色可辨。
  rect=十字+方块、ellipse=十字+椭圆、arrow=十字+箭头、line=十字+斜线、
  brush=十字+实心圆、mosaic=十字+田字格、number=十字+单环、text=I 形、select=系统箭头。
- 注意：memory 日志里写反引号代码段时 bash 会做命令替换吞内容，用 python ` 转义写。
- 验证：tsc ✓ vite build ✓ CRLF ✓。commit 6b3142b push 成功。
## 箭头粗线尖头不消失 + 画笔虚线框可拖动（72df21a，已推送）

- **箭头尖端消失根因**（主人：画笔太粗，箭头图形的尖就消失了）：旧头是两条线段，
  头长固定 16px 不随线宽放大，粗线时主线方头/圆头在 X2 与头线段叠成钝块 → 尖端被吞。
  修复为**填充三角**：`closePath+fill` 同色实心三角，头长随线宽等比放大
  `Math.max(12*scale, w*3.2)`（细线保底 12、粗线不缩水），上限 `len*0.42`；
  半张角 0.46rad ≈ 26.4°（Snipaste 式锐角）；fill 后同色 lineWidth=1 细描边
  消除填充边缘与底色间的抗锯齿细缝。任何粗细下尖端都锐利。
- **画笔虚线框拖不动根因**：shapeHit 对 brush 按「笔迹点到线段距离」判定——
  虚线框内空白处漏判 → pointerdown 落到空白就当新笔画落笔，图形自然拖不动。
  修复为**包围盒判定**：`annoBounds` 框内即命中（含 tol 外扩），框内=移动、
  点框外空白才落新笔。
- **二次编辑与当前工具解绑**：装饰显示与 onDown 拦截原要求
  `es.kind === tool`（编辑对象须与当前工具同种）→ 切工具后上一笔虚线框消失、
  也拦截不到。改为 `tool !== 'select'` + `shapeEditable(es)` 即可：任何绘制工具下
  上一笔框内都能拖（select 仍走选区逻辑不冲突）；OCR 划选期间隐藏不受影响。
- 验证：tsc ✓ vite build ✓ CRLF ✓ diff 复核 ✓。连同 784c43c（光标 32×32 升级，
  上一轮漏推）一并 push：f07de3a..72df21a。临时文件 tmp-cursor-preview.html 已删。
## 箭头粗线尖端变「线头」根因修复（a872e02，已推送）

- **主人复测反馈**：上版虽改填充三角，但「画笔太粗时箭头不尖锐，线穿透了箭头、
  箭头变成线头」。
- **真根因**：主线仍 `lineTo(X2, Y2)` 直画到尖端，且 `lineCap=round`——圆帽以
  X2 为圆心、半径 w/2 向前凸出；填充三角顶点也在 X2、**盖不住尖端前方**的圆帽
  凸出部分 → 粗线（w≥20，半径≥10px）时尖端整体是半圆弧，两侧还从三角边露出，
  视觉 = 圆头「线头」。
- **修复**（drawShape arrow 分支重写）：
  1) 主线【不到尖】：止于 X2 向内 `hl*0.6` 处（`X2 - ux*inset`），round 圆帽
     完全缩进头内、尖端前方纯属填充三角；2) 头改【平底等腰三角】而非楔形张开：
     尖在 X2、基座在 X2 后 hl、基座半宽 `bw = max(w*1.1, 4*scale)` 独立可控——
     恒宽于杆(半宽 w/2)，圆帽两侧不外露；3) `hl = min(max(12*scale, w*3), len*0.45)`。
- **几何自验**（node 数值推演 7 个 case 全过）：w=24/len=300 → 圆帽前缘距尖端
  31.2px 纯三角段、圆心深度处三角半宽 15.8 ≥ 半径 12；细线 w=2 由 4*scale 保底。
- 验证：tsc ✓ vite build ✓ CRLF 纯 LF=0 ✓。预览页 tmp-arrow-preview.html（源码
  同公式 + 尖端放大 3.8× 对照旧版）供主人目检，确认后删除。push fe97445..a872e02。
## 贴图(pin)拖动卡顿排查与修复（本轮）

- **主人现象**：① 贴图刚贴上立刻拖动卡顿；② 有时贴了很久也明显卡。
  主人猜测「创建元素慢，能否预建备用元素 + 空闲补充」。
- **事实澄清**：备用池机制【早已存在且工作正常】——pin.rs 有
  `STANDBY_COUNT=2` 张屏幕外(-32000)**可见**待命窗（合成器预热，移动即显示）、
  `pin_busy/INTERACTING + wait_idle()` 空闲补建。diag.log 实测贴图链路
  attach→img decoded(+24~36ms)→ready **仅 50~70ms**，创建本身不慢。
- **真根因（问题①）**：`pin_ready` 里补建待命窗是 `ensure_staging(&app)`
  【立即执行、不等空闲】——而贴图刚上屏正是用户最可能马上拖的时刻，
  WebView2 建窗是主线程重活（defer_to_main_loop，数百 ms 阻塞），
  用户起手拖动直接撞上 → "刚贴上立刻拖卡一下"。
  （对比：attach_to_staging 的补建有 wait_idle(8000)，两处不一致 = 遗留遗漏）
  修复：改为 spawn 线程先 sleep 900ms 起手保护期 + wait_idle(8000) 再补建。
- **真根因（问题②）**：非逻辑 bug，是每帧合成成本——透明窗每次移动都要 DWM
  重合成整窗，叠 `.pin-shadow` 两层 box-shadow 后每帧多出数百万像素模糊采样，
  大贴图（4K 选区）尤甚。修复：拖动中（.pin-dragging）卸掉 --pin-drop 投影，
  松手自动恢复（特异性 0,4,0 > .pin-shadow 0,3,0，无需 !important）。
- **附带修复**：拖动起手不再等 `outerPosition()` 的 IPC 往返（此前 ready=false
  期间所有 pointermove 被丢弃 → 起手发涩）。新增 `posRef` 位置缓存，由
  onMoved 事件 payload【零 IPC】维护，mousedown 即用缓存起步。
- **新增可验证手段**：拖动帧耗时埋点，松手打
  `[pin] drag Nf avg=..ms p95=..ms max=..ms` 到 diag.log（>32ms 即掉帧）。
- 验证：tsc=0 ✓ cargo check ✓（20.5s 无警告）✓ vite build ✓。
### 续：找到「松手后卡一下」的确凿根因（e2bc068）

- **`pin_update` 无条件动窗口**：`w.set_position(win_pos(x,y))` +
  `w.set_size(win_size(w,h))` 每次都执行。而前端 `persistNowRef` 在
  **每次拖动松手（onMoved 防抖 400ms）+ 每次 mouseup 监听** 都会用当前几何
  回写持久化 → 几何一字未变却照样 set_size → 触发 WM_SIZE → 整页重新布局
  + 重新光栅化（4K 贴图可达数百毫秒）= 主人说的"贴了很久也卡、很明显"。
- 修复：改【变化才动】——先 `outer_position()/inner_size()` 比对目标几何，
  无变化则一次窗口操作都不做；`set_ignore_cursor_events` 同样只在
  click_through 真正翻转时才调（改窗口样式会触发重绘）。
- **通用教训**：任何「用当前值回写持久化」的路径都不要顺手重设窗口几何，
  必须先比对——这类"幂等但昂贵"的调用正是隐性卡顿的高发区。
### 续：找到「松手后卡一下」的确凿根因（e2bc068）

- **`pin_update` 无条件动窗口**：`w.set_position(win_pos(x,y))` +
  `w.set_size(win_size(w,h))` 每次都执行。而前端 `persistNowRef` 在
  **每次拖动松手（onMoved 防抖 400ms）+ 每次 mouseup 监听** 都会用当前几何
  回写持久化 → 几何一字未变却照样 set_size → 触发 WM_SIZE → 整页重新布局
  + 重新光栅化（4K 贴图可达数百毫秒）= 主人说的"贴了很久也卡、很明显"。
- 修复：改【变化才动】——先 `outer_position()/inner_size()` 比对目标几何，
  无变化则一次窗口操作都不做；`set_ignore_cursor_events` 同样只在
  click_through 真正翻转时才调（改窗口样式会触发重绘）。
- **通用教训**：任何「用当前值回写持久化」的路径都不要顺手重设窗口几何，
  必须先比对——这类"幂等但昂贵"的调用正是隐性卡顿的高发区。

### 续：贴图拖动 200~800ms 单帧冻结 —— wait_idle 缺系统输入判定（真根因）

- **证据**：新写 scripts/pin-drag-stats.py 解析 diag.log 的 `[pin] drag Nf avg=.. p95=.. max=..`
  埋点，跑出 9 条记录：avg 只有 8~16ms（说明拖动本身很流畅），但 **max 平均 251ms、
  最差 816.8ms，6/9 次掉帧**。=> 不是持续卡顿，是【单次冻结】，且冻结与拖动起手高度相关。
- **根因**：补建待命窗前的 `wait_idle()` 只查前端上报的 `INTERACTING` 标记。
  补建线程 250ms 轮询时，用户常常【还没按下鼠标】-> 判定空闲 -> 开始建窗；
  WebView2 建窗 + 加载整个前端应用是**主线程数百毫秒且不可中断**的重活，
  用户恰恰在这段窗口里起手拖动 -> 单帧被拉到 200~800ms。
  （前端标记只能覆盖「已经按下之后」，覆盖不了「按下之前的意图」。）
- **修复**：`wait_idle` 改双重条件 —— ① INTERACTING 为假 且 ② 系统级输入静止
  >= 350ms。新增 `last_input_idle_ms()`（Windows 走 GetLastInputInfo + GetTickCount，
  覆盖本进程之外的全局输入；非 Windows 返回 u32::MAX 视为空闲），常量
  `INPUT_IDLE_MS = 350`，探测间隔 250ms -> 100ms。
- 同时把 `attach_to_staging` / `pin_ready` 两处 `wait_idle(8000)` 放宽到
  `wait_idle(30_000)`：实测一次拖动可长达十几秒，8s 上限会超时硬建窗，
  等于把保护作废。超时兜底仍保留（30s 后无条件建），避免用户一直动导致待命池长期空缺。
- **通用教训**：判断「用户现在忙不忙」，**进程内标记不够，必须叠加系统级
  输入空闲（GetLastInputInfo）**。标记只能反映已经开始的交互，系统输入时间戳
  才能覆盖「即将发生的交互」。凡是要在空闲时执行的不可中断重活（建窗、大 I/O、
  重编译），都应走这套双重门控。

### 续：贴图 OCR 弹窗翻译后没变大 + 翻译失败行不显（commit 151b00a）

**问题 ①：贴图 OCR 弹窗（独立 pin-ocr 窗）翻译后没自动变宽**

- 截图弹窗在同 WebView 内只切 CSS 就自动变大；贴图弹窗是独立 WebView 窗口，
  CSS 宽度变化不会自动同步到 OS 窗口尺寸。
- 必须由 useLayoutEffect 重新量尺寸 + setSize 才能让 OS 窗口真正变宽。
- 真根因：早返回检查只比较 mh，**宽度变化但 mh 未变时漏掉 setSize**，
  看起来窗口纹丝不动。
- 修复：
  - 加 PANEL_W_TRANS = 560 常量；transMode（trans 非空 || translating）
    时 inline style width 切 300 → 560，与截图弹窗的 320 → 560 对齐
  - useLayoutEffect 加 lastWRef 缓存上次窗口宽；早返回检查纳入 mw

**问题 ②：翻译后「原文 == 译文」看起来都是失败——两种本质不同的情形混在一起**

- ① 服务商原样返回：纯数字 / 标准号无可译内容 → has_cjk=false → target=zh，
  API 没东西可译返回 src，pr.ok=true。**这是 API 正常行为**。
- ② API 调用失败：网络 / 配额 / 行数超出 LINE_BLOCK_BUDGET(24) → 块请求折叠
  退回 per-line 仍超时 → pr.ok=false 回退 src。
- 原 .ocr-pfail 把译文列压暗到 0.42 透明度，差异太弱不易察觉，主人看不出
  哪些是真的失败哪些是「本来就没什么可译的」。
- 修复：失败行加 .ocr-pfail-tag 内联红色「未译」标签，仅在 !ok 时挂载，
  让两种情况视觉上一眼可辨。

**通用教训：**
- 独立 WebView 窗口的尺寸同步 = 量 DOM + setSize，**不能依赖 CSS 切宽**；
- 「幂等但昂贵」的早返回检查要覆盖所有可能变化的维度（这次只查了高度漏了宽度）；
- 状态布尔（ok=true/false）要在 UI 上有明确视觉表达，仅靠透明度差异不够；
  服务商返回原值和调用失败在数据层是两种状态，UI 必须能区分。

## 全局快捷键在 Electron/Chromium 系应用内失效（裸功能键走低级钩子，本次）

- **主人现象**：`shortcuts.screenshot = 'F1'` 在 WorkBuddy / VS Code 这类 Electron 应用里
  完全没反应（尤其截图），但在其他应用里正常。diag.log 铁证：在 WorkBuddy 按 F1
  无任何日志，其他应用里 `[shortcut] pressed F1 → screenshot triggered` 正常。
- **真根因**：`F1` 是裸功能键。原 `is_hook_combo` 只认 `SUPER`（Win+）和 `ALT`（Alt+）
  组合，裸键走 Tauri `global_shortcut`（底层 RegisterHotKey，靠系统发 `WM_HOTKEY`）。
  而 Electron/Chromium 应用在自身消息循环里**自行消费 F 键、不交给 DefWindowProc**，
  `WM_HOTKEY` 根本不生成 → RegisterHotKey 在这些应用里失效。
  （对比：WH_KEYBOARD_LL 低级钩子在事件到达任何应用之前就接管，天然免疫。）
- **修复**（keyhook.rs + shortcut.rs）：
  - `HookMode` 枚举（`Win`/`Alt`/`Bare`）替代原 `is_alt: bool`；新增 13 个
    `*_BARE_VK` 槽位（仅 F1~F12）。
  - `hook_proc` 加**裸功能键分支**：要求 `!win_held && !alt_held && !CTRL_HELD &&
    !SHIFT_HELD`（所有修饰键都未按住）才拦截，命中即吞键 post 对应 Action。
  - `is_hook_combo` 纳入 `mods 为空 && is_function_key(key)`；`register_combo`
    对 Alt **和** Bare 都额外 `global_shortcut().register` 做兜底。
  - `unregister_all_runtime` 清三套槽位；`shortcut_test` 对裸功能键也走真注册检测占用。
  - `hook_vk` 报错文案改为「钩子接管的热键仅支持字母、数字与 F 键」。
- **关键约束**：**仅功能键（F1~F12）可裸键**——裸字母/数字键会致用户打字打不出字符，
  绝对禁止路由到钩子吞键；裸键分支必须强校验 `is_function_key` 且所有修饰键未按住。
- 验证：`cargo check --message-format short` 无警告。改 Rust 需重编译，主人需重新
  `tauri dev` 后在 WorkBuddy 内按 F1 验证（diag.log 应出现 `[keyhook] bare hotkey
  matched vk=0x70`）。

## 贴图 OCR 翻译弹窗「另一侧缩没」二次修复：独立窗 maxWidth 的 100vw 自锁（本次）

- **主人复测反馈**：上一轮（151b00a）只解决了「弹窗变大」却没考虑弹窗在贴图两侧
  都会出现；在另一侧（贴图左侧）翻译时面板不但没变大、反而「直接就缩没了」。
- **真根因（比上轮更深一层）**：151b00a 给 OcrPanel 加了
  `maxWidth: min(560px, calc(100vw - 16px))` 防超屏——这行在**截图侧（全屏遮罩窗）
  正确**（vw=全屏宽 → 560 恒成立），但 **pin-ocr 是独立 WebView 窗，vw = 窗口自身
  视口宽（创建时 width:300）** → 翻译后 maxWidth 被 clamp 成 min(560, 284)=284，
  面板实际宽被压回 284；useLayoutEffect 量 offsetWidth=284 → setSize(284) →
  窗口从 300 反而**缩窄**，两列译文依旧挤在 284px——左右两侧任何一侧都中招，
  观感正是「缩没了」。截图侧主人觉得正常，恰好因为它在全屏窗里 vw 够大。
- **修复**（PinOcrWindow.tsx）：
  - 宽度上限从 `100vw` 改为**屏幕可用宽**：render 里 JS 算单一事实源
    `panelW = transMode ? max(PANEL_W, min(PANEL_W_TRANS, round(availW)-16)) : PANEL_W`，
    style 的 width/maxWidth 都用 panelW——独立窗内不再有任何依赖视口宽单位的自锁；
  - useLayoutEffect 量出 mw/mh 后先 clamp 屏幕容量（availW-4/availH-4）再 setSize，
    防御高缩放小屏下 DOM 目标宽 > 屏宽导致坐标 clamp 失效、窗口溢出屏外；
  - 选侧/翻侧逻辑（right/left/inside + 屏内 clamp）保持——扩宽后按新宽重算 x，
    无论贴图在屏内哪侧、面板在贴图左/右/内，窗口都完整落在屏内不溢出。
- **通用教训**：**CSS 视口单位（vw/vh）在独立小窗口里是「窗口自身视口」不是屏幕**；
  凡独立 WebView 窗需要「防超屏」的上限约束，一律用 `window.screen.availWidth/Height`
  在 JS 里算好再注入 style，绝不能抄全屏窗里的 `calc(100vw - 16px)` 写法。
- 验证：tsc ✓ vite build ✓ CRLF 纯 LF=0 ✓（diff 仅 PinOcrWindow.tsx，18+/8-）。

## 贴图过大时 OCR 翻译弹窗「不显示」修复：overlap 硬置顶（96e021a 后续，本次）

- **主人反馈**：贴图过大（占屏绝大部分）→ 翻译弹窗两侧都没空间 → 直接不显示了。
  主人期望：弹窗不能被挤掉，最多靠屏幕边，**与贴图重合也无所谓，总不能不显示**。
- **真根因**：选侧逻辑在两侧都放不下时正确退到 inside（贴图内/左上角内缩）兜底，
  setSize/setPosition/show 全都执行了——但**贴图窗是置顶窗**，主人滚轮放大/拖拽贴图
  时贴图窗被激活到置顶链顶端、z 序压在 pin-ocr 之上；弹窗一旦与贴图矩形重叠
  （贴图巨大时必然），就被贴图窗整个盖住 → 视觉「不显示了」。外侧无空间时
  overlap 是常态，之前代码从未处理过重叠场景的 z 序。
- **修复**（PinOcrWindow.tsx）：定位后判弹窗矩形与贴图矩形是否相交（overlap）；
  相交则把本窗硬置顶 `win.setAlwaysOnTop(true)`（底层 SetWindowPos HWND_TOPMOST，
  提到置顶链顶端且 NOACTIVATE 不抢焦点），250ms 节流避免贴图拖拽/缩放期间
  geometry 事件高频到达时反复 SetWindowPos 拖慢帧率。弹窗在贴图外侧（正常形态）
  时不动 z 序，不干扰贴图交互。
- **通用教训**：两个 alwaysOnTop 窗重叠时，**后激活者在上**——弹窗与被操作窗
  必须重叠时，光 show/setPosition 不够，要显式 SetWindowPos(HWND_TOPMOST) 把自己
  提到置顶链顶；激活方每次交互后都会再压上来，交互期需节流重顶兜底。
- 验证：tsc ✓ vite build ✓。

## 贴图后 OCR 弹窗不随贴图关闭 + ✕ 失效 + Esc 生效（fb67e96 后续，本次）

- **主人反馈**：前两轮修复只覆盖「未贴图/截图」路径，贴图后 Alt 文字识别→翻译
  场景问题依旧；且新增关键 bug——Alt 文字识别激活状态**关闭贴图**时 OCR 弹窗
  （pin-ocr 独立窗）不关闭；对弹窗**手动点 ✕ 也不生效**；但 **Esc 退出生效**。
- **真根因**（两层）：
  1. pin-ocr 是前端动态创建的**独立 WebView 窗**，来源贴图窗 close/Alt+F4
     **不会级联关闭它**；前端安全网（getByLabel(data.pin) 为空才自毁）本可兜底，
  2. 但贴图窗销毁统一走 staging 机制——**staging 待命窗被销毁后 ensure_staging
     立即补建同 label 的新窗**，`getByLabel` 永远非空 → 安全网被**骗过**，孤儿
     弹窗带着旧贴图脏 data 跨贴图残留复用 → 这正是前两轮「贴图后问题仍在」的
     持续来源；残留窗处于异常交互态（源已销毁，✕ 的 close 事件发往不存在对象）
     → 手动 ✕ 失效。Esc 走 exitMode+pinClose→closePinOcrWindow（前端销毁路径）
     与命令路径并存所以生效。
- **修复**（Rust 硬兜底 + 前端双保险）：
  - `pin.rs`：`pin_close` 命令内（关窗前）调 `drop_ocr_window(&app)`；新增
    `pub(crate) fn drop_ocr_window`——销毁 label "pin-ocr" 窗，幂等。
  - `lib.rs` WindowEvent::Destroyed 统一出口：staging 分支 + 常规贴图窗分支
    （`pin-` 前缀且排除 `pin-ocr`/`pin-menu`）都调 `drop_ocr_window`，
    再 `forget_pin`——Alt+F4 直关贴图窗也覆盖。
  - `PinOcrWindow.tsx`：`selfClose` 改双保险——立即 destroy + 300ms 延迟再
    destroy 一次（吞掉与来源 hide 的异步竞争）；安全网轮询 300→200ms。
- **通用教训**：动态子窗若依赖「来源存在性」做自毁安全网，必须防「来源销毁
  即被同 label 补建」的欺骗——子窗级联关闭应下沉到**父窗销毁的 Rust 统一出口**
  硬兜底，前端安全网只当第二道闸。另：孤儿窗「手动关失效但快捷键生效」的
  特征，指向关闭事件发往了已销毁对象，而非按钮事件本身没触发。
- 验证：cargo check ✓（21s）tsc ✓ vite build ✓（6.7s）。

## 贴图 OCR 弹窗定位重构：贴屏边最小重合 + 周期置顶 watchdog（ecd439b，本次）

- **主人反馈**：贴图过大时弹窗仍"被挤掉"，且期望——空间不够时尽可能靠屏幕边、
  与原始贴图**重合尽量少**。
- **真根因（两层，首轮硬置顶没根治的原因）**：
  1. 兜底策略只看左右两侧，放不下就退"贴图内左上角"，与巨大贴图几乎整块重合；
  2. **补顶是一次性的**——setAlwaysOnTop 只在布局 effect 时机调；贴图窗同为置顶窗，
     拖拽/滚轮把它激活到置顶链顶端，且交互结束的"最后一次激活"没有几何推送来
     触发补顶 → 弹窗**停在被盖状态**。
- **修复**（PinOcrWindow.tsx 定位逻辑整体重构）：
  - 候选求优选位：① 贴图外侧**四向**(右/左/下/上)余量足够一侧(零重合、当前侧
    不翻侧) → ② 四向全不够 → 展开全部候选（外侧姿态 + **贴屏幕四角**）取与贴图
    **重合面积最小**、并列取离当前最近——不再存在"退贴图内"形态；
  - **周期置顶 watchdog**：overlapRef + doRaise + 200ms setInterval——重合期间
    持续 SetWindowPos(HWND_TOPMOST, NOACTIVATE)，任意交互后 ≤200ms 回到置顶链顶。
- **仿真自验 5 场景**：常规首放→右侧零重合；翻译扩宽(右放不下)→下方零重合；
  巨大贴图贴左上→右下角贴边 52% 重合(理论最小)；巨大居中→角上 5084px² 极小重合；
  占满屏→贴角可见。均符合预期。
- **通用教训**：置顶窗互相遮挡时，"激活方交互结束的最后一次激活"往往没有事件通知
  触发重顶——**单次补顶必然失效，需重叠期间周期补顶**；定位兜底应优先"贴屏边 +
  面积最小重合"而非"覆盖到被操作对象内"。
- 验证：tsc ✓ vite build ✓（7.1s）；node 仿真 5 场景 ✓。

## WorkBuddy 里 F1 裸功能键失效：修饰键 keyup 被系统吞致 ALT_HELD 残留（4c1021b）

- **主人症状**：WorkBuddy（Electron，普通权限启动）前台时**全部工具箱快捷键失效**
  （a8aa962 低级钩子修复后仍无效）。
- **受控测试定位**：WorkBuddy 前台按 F1 → diag **无** `bare hotkey matched`；
  按 Alt+V → **有** `alt hotkey matched` 正常呼出。同一低级钩子，能收到 Alt+V
  必然也收到 F1——差异只在裸键分支要求 `!alt_held && !ctrl_held && !shift_held
  && !win_held`。
- **真根因**：Alt+Tab（diag 里 08:23~08:28 密集 `alt+key vk=0x9`）后**系统吞掉
  Alt 的 keyup** → 内部 ALT_HELD 残留 true → 裸功能键永远被"有修饰键按住"挡掉；
  Alt+V 只要 Alt down 置位即匹配所以正常。进 WorkBuddy 前常 Alt+Tab → 表现为
  "只在 WorkBuddy 里失效"。残留还会让纯字母被误判成组合（输入 v 呼出剪贴板）。
- **修复**（keyhook.rs）：新增 `reconcile_modifiers()`——每个事件以
  GetAsyncKeyState 物理状态校准四修饰键标记，只清除"标记 true 但物理已松开"
  的残留、绝不反向误置，校正写 self-heal 日志；hook_proc 修饰键分支前调用。
  另加 F1~F12 未匹配诊断（`fn-key unhandled` + 四标记），区分"钩子没收到键"
  与"状态残留挡掉匹配"。
- **通用教训**：低级钩子自维护修饰键状态时，**系统会吞 keyup 的坑位**：
  Alt+Tab 松 Alt、Win 菜单/贴靠松 Win——必须用 GetAsyncKeyState 定期校准
  或事件即时对账，否则"特定组合/裸键只在某些操作序列后失效"且极难复现。
- 验证：cargo check ✓ 无警告（1m11s）。需主人重编 dev 实测。
- 收尾：MEMORY.md 追加 keyhook 修饰键物理自愈铁律（GetAsyncKeyState 对账只清残留），
  commit a0eb101 已推送；仓库干净，本地=远程。提醒：MEMORY.md 现 3865 字符超限，下次蒸馏。
- 需求：呼出翻译面板自动聚焦原文输入框（2ff9664，tsc/cargo check 通过，已推送）。
  方案：前端 focusSourceInput(光标置末尾)+scheduleSourceFocus 双时序(60/240ms，后者抵消
  activate_popup 后台 120ms 补焦重置)；有选中走 translate://start 回调聚焦，无选中路径
  Rust 补发轻量 translate://shown 事件(不带内容不清空)，顺带补齐无选中呼出的 lastStartAt 失焦守卫。
- UIPI 修复(4f924a0,已推送)：main.rs 入口自提权后，普通终端 tauri dev 会被 UAC
  重启中断(dev CLI 误判 app 退出)。修复:is_dev_spawn() 用 ToolHelp 快照沿父进程链
  上溯 6 层，命中 cargo.exe/tauri.exe 判定 dev 拉起 → 跳过自动提权，普通权限跑 dev
  不断链;双击/自启 exe(父链 explorer/宿主)照常 runas 提权。快照失败回退提权(安全)。
  使用矩阵:普通终端 dev=普通权限;管理员终端 dev=已高直接 return;exe 双击/自启=自动提权。
  Cargo.toml 增 Win32_System_Diagnostics_ToolHelp feature。autocrlf=true 下 Edit 工具
  写出的 LF 文件(如 Cargo.toml)需手动转 CRLF 否则 git 报 needs update 误报。
