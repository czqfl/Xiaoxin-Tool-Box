# 2026-08-23 工作日志

## 快捷键推倒重来式重构，根治改键残留/卡死（commit e2a9daa，不推送）
- 主人反馈：快捷键「改键后新键不生效 + 旧键一按卡死」；改回原键也不生效。
  怀疑旧配置残留，要求"完全删掉原有快捷键逻辑重写"。
- 诊断过程：
  1. diag.log 实锤：主人今天测试的是**含 screenshot/pins（截图/贴图）的未提交新功能**
     版本（源码 15:38 改、exe 15:40 构建），git 里 screenshot/pins 是未提交工作树。
  2. 三份配置（dev target/debug/data、项目根 data、%APPDATA%）shortcuts 各异，
     存在历史遗留 target 字段（screenshot/pins）；用户曾把功能键设为纯 F6
     （前端已允许 F1~F12 单独使用，如 F1 截图）。
  3. 旧 apply 流程「先注册新键→成功才注销旧键」：旧键注销环节一旦出岔子
     → 新旧混存（旧键继续触发=卡死、新键被旧键占位不生效）。
- 重写（src-tauri/src/shortcut.rs）：
  - `shortcut_apply`：校验 → **全量注销**（插件 unregister_all + keyhook 钩子槽位
    清零 + 绑定表清空）→ 按更新后配置**全部重建** → 任一失败**自动回滚**原配置
    重建并返回错误。改键=全新状态，运行时与配置严格一致，无新旧混存。
  - 新 helper 消灭 if-else 地狱：TARGETS 常量表 / config_shortcut /
    set_config_shortcut / set_binding / register_one_result / resync_all_result。
  - register_initial / resync_all 统一走 resync_all_result；启动失败留痕+开设置页。
  - 删除不再需要的 unregister_combo。
- 前端配合已有：设置页有「重置并重新注册全部快捷键」按钮（shortcut_resync）+
  「运行时实际生效」展示（shortcut_runtime_bindings），脱节一眼可见。
- 提交含未提交的截图/贴图功能（pin.rs/screenshot.rs/前端模块）——shortcut.rs
  依赖它们，必须一并沉淀保证仓库可编译。
- 验证：cargo check + tsc 通过。
- 经验：热键"改键后新旧混乱/卡死"优先怀疑**注销环节失败残留**；根治方案不是
  补注销，而是 apply 全量重建（清空→按新配置注册→失败回滚），从结构上杜绝。

## 「F6 卡死」根因定位 + 截图遮罩看门狗（commit 8ce52fa，不推送）
- 主人：设 F8 不生效、按旧 F6 一按卡死。
- 证据链（diag 实锤）：
  1. **F6 从未被应用收到**（无 pressed F6 记录）→ F6 当前根本没注册
     （config 无 F6；全清重建后已注销）。F6 卡死不可能是注册逻辑。
  2. F8 = pins（贴图），已注册且 pressed F8 → pins toggle 触发正常，
     只是无贴图时 `toggle skipped: no pins` 无可见效果 → "F8 不生效"错觉。
  3. 07:59:22 按 F5（screenshot）begin ok 后无输出/取消记录 →
     截图全屏遮罩（置顶吞输入）处于打开/残留状态 → 吞掉所有键盘鼠标 =
     "按 F6 卡死"的真实来源。历史注释已记录此坑（POST 上传挂起→遮罩永不隐藏）。
- 修复：begin_impl 遮罩就绪后启动 **60s 看门狗**（SHOOTING 仍 true 且遮罩
  可见→强制 hide_all + 复位），从根上杜绝遮罩残留卡死桌面。
- 经验：用户报"某键卡死"先查 diag 有无 pressed 记录——没有=该键没注册，
  卡死另有来源（全屏遮罩/模态窗口吞输入是常见元凶）；截图类全屏窗口必须
  有超时看门狗兜底。

## 截图选区按 F8 直接贴图 + F6 真相（commit 806ddc9，不推送）
- 主人澄清真实需求：①F6 是啥、要去掉；②截图选区确定后按 F8 直接贴桌面
  （替代手动点右下角"贴图"按钮）。
- F6 真相：当前配置没有任何 target=F6（应用完全不响应 F6）；diag 无 F6 记录。
  按 F6 卡 = 其它软件/系统占用 F6（全局热键冲突），非本应用键。
- 实现：遮罩前端 phase=selected 按 F8 → doOutput('pin')（与既有 Ctrl+T 等价）；
  Rust 截图会话中（screenshot::shooting()）全局 F8 忽略——F8 语义临时切为
  "贴图"而非"显示/隐藏贴图"，防止 RegisterHotKey 全局触发与遮罩冲突
  （刚贴的图被 toggle 隐藏）。平时 F8 仍是 toggle，语义不变。
- 验证：cargo check + tsc 通过。
- 经验：同一物理键在不同模式有不同语义时，用全局状态（shooting）在 Rust
  handler 里按模式分流，前端聚焦窗口接管该键；注意 RegisterHotKey 全局触发
  优先级高于窗口焦点，必须后端屏蔽而非只靠前端监听。
